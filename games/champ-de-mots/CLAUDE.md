# Le Champ de Mots — French Syllabus Farming Game
### Design Doc v0.1 (Personal project — not tied to a BCM assessment)

---

## 1. Concept

A farming/idle-management game where **plots = vocabulary and grammar points** from FREN151 and FREN152, planted and grown in the same order your syllabus teaches them. The healthier a plot, the less manual attention it needs — mirroring how spaced repetition works in real memory: shaky new knowledge needs constant watering, well-committed knowledge runs on autopilot.

**Core hook:** you're not managing an abstract farm, you're literally watching your own French knowledge become self-sustaining.

Working title: *Le Champ de Mots* ("The Field of Words") — open to renaming.

---

## 2. Core Loop

1. **Plant** — a new unit unlocks a batch of plots (vocab words, a conjugation pattern, a grammar rule).
2. **Tend** — each plot needs periodic "watering," which is really a recall/practice prompt (translate, conjugate, fill-in-blank, listen-and-type).
3. **Grow** — correct, spaced-out recall grows the plant through visible stages.
4. **Automate** — once a plot's mastery score crosses a threshold, it gets an auto-waterer (sprinkler icon), meaning it needs far less frequent manual check-ins, but still occasionally resurfaces (like real spaced repetition never truly stops).
5. **Expand** — automating plots frees up your daily "attention budget" to plant the next syllabus unit's words, so the farm visually grows in step with your real course progress.

This loop **is** a lightly-skinned spaced repetition system (SRS) — the game layer's job is to make SRS visually satisfying instead of feeling like flashcard homework.

---

## 3. Plant Growth Stages

| Stage | Visual | Meaning |
|---|---|---|
| Seed | Bare soil, seed packet icon | Just introduced, not yet tested |
| Sprout | Small green shoot | Recalled correctly once |
| Budding | Stem with leaves | Recalled correctly across 2+ spaced sessions |
| Blooming | Flower/fruit forming | Strong recall, longer intervals passed |
| Harvested / Automated | Full plant + sprinkler icon | Mastered — auto-watered, drops to rare "maintenance" reviews |
| Wilting | Drooping, greyed | Overdue for review — visual guilt-trip, gently nudges you back |

Wilting is deliberately **not punishing** (no plant death) — this is a learning tool, not a Duolingo-streak-guilt machine. A wilted plant just needs a re-water to recover; nothing is ever lost permanently. (Worth flagging: this is a personal-wellbeing design choice, not just a game-design one — I'd avoid any "you failed / start over" framing.)

---

## 4. Content Model — Continuous FREN151 → FREN152 (complete)

Per your call, FREN151 and FREN152 are **one continuous farm**, not two seasons. The content model reflects that: every week across both subjects gets a single incrementing `sequence` number, while `course` and `week` are kept as metadata so the game can still show "FREN151, Week 3" style labels if useful.

```
Combined timeline (fren_combined_catalog.json)
 └─ Week entry: {course, week, sequence, chapter, topics[]}
     └─ Topic (vocab | grammar | phrase | phonetic)
         └─ Item (fr/en pair, or prompt/answer for phonetics)
```

**Final tally: 23 weeks, 138 topics, 1047 items**, sequence 1–23 running straight through FREN151 (weeks 1–11) into FREN152 (weeks 2–13 — FREN152's own "week 1" has no separate tutorial content; it's folded into its week 2 as bridge material, so the catalog picks up there). (Grew from the original 129 topics/966 items via a slideshow cross-check pass — see the "Slideshow cross-check merge" build note below.)

| Segment | Sequence | Chapter theme |
|---|---|---|
| FREN151 Ch.1–4 | 1–11 | Greetings → family & description (A1) |
| FREN152 Bridge | 12 | Numbers/time revision, present tense -ir/-re |
| FREN152 Ch.5 | 13–15 | Daily routine, reflexives, comparative/superlative |
| FREN152 Ch.6 | 16–18 | Shopping for clothes, colours, demonstratives |
| FREN152 Ch.9 | 19–23 | Food & drink, partitive, passé composé |

The farm's rows keep extending in sequence order — a plot planted in FREN151 week 3 sits to the left of one planted in FREN152 week 5, with nothing marking the subject boundary except the chapter label. That's the point: it reads as one course of study, matching how you're actually learning it.

**Important: the catalog contains only facts (vocab pairs, grammar rules), never the workbook's own exercises, dialogues, or scrambled-word puzzles.** Per your instruction, practice questions are generated at runtime from the catalog data rather than reproduced from the textbook — see Section 5.

---

## 5. Practice Mechanics (the "watering" actions)

**Practice questions are always generated at runtime from the catalog data — never copied from the workbook's own exercises.** This does two things: it stops practice from turning into "memorise this one specific fill-in-the-blank answer," and it keeps the game clear of the workbook's copyrighted exercise material (the catalog only stores vocab pairs and grammar facts, which the game recombines freely).

Generation approach per `topic_type`:

- **vocab / phrase** — translate prompts (FR→EN or EN→FR, alternated), with multiple-choice distractors pulled from *other items in the same or a nearby topic* (so distractors are plausible, not random). The direction and the distractor set are re-rolled each time a plot is watered, so the same word never has one fixed question.
- **grammar** — fill-in-the-blank generated by taking one of the rule's example items and blanking the target word/ending; conjugation items get a pronoun swapped in from the six-person set so the blank changes each visit.
- **phonetic** — matching/multiple-choice between letter-or-symbol and its spoken name.

Each plot's practice type is determined by its `topic_type`. A pool of at least 3-4 plausible variants per item is the design floor — enough that watering the same plot twice rarely gives an identical question.

---

## 6. Spaced Repetition Engine (the actual mechanic under the hood)

Lightweight SM-2-style scheduling, not the full algorithm — simplicity matters more than academic rigor here:

- Each plot tracks: `ease_factor`, `interval_days`, `last_reviewed`, `next_due`, `correct_streak`.
- Correct recall → interval grows (e.g., 1 day → 3 → 7 → 14 → 30...).
- Incorrect recall → interval resets to short (1 day), streak resets, but **no visual punishment** beyond "this plot needs water again."
- Crossing an interval threshold (e.g., 14+ days) flips a plot to "Automated" — it still reappears eventually, just far less often.

This state is per-plot, per-user — fits your existing save-code system (Neon/Postgres) rather than needing a new backend.

---

## 7. Farm Expansion & Syllabus Pacing

- The farm map is laid out in **rows = sequence number** (1–23 currently), left to right, running continuously across FREN151 into FREN152 with no reset.
- A new row of plots unlocks only once the *previous* row's plots have all reached at least "Sprout" — this keeps the game pace loosely tied to your actual course pace rather than letting you binge-plant everything at once (which would defeat the spaced-repetition point).
- Since FREN151 is already complete, its 11 rows can be marked as available immediately (a "catch-up" zone) while FREN152's rows unlock week by week as the semester progresses.

---

## 8. Visual/UX Notes

- Static, screenshot-friendly presentation matches your stated preference — the "farm" can be a simple grid/SVG scene with plant-state sprites swapped per stage, no animation required for assessment-style screenshots (not that this needs assessment, but keeps build effort low either way).
- No forced daily-streak pressure UI (no fire emoji guilt) — a calm "plots needing water today" counter is enough nudge.

---

## 9. Tech Fit (matches existing hub stack)

- Pure HTML/CSS/JS or Pyodide, no build step — consistent with the rest of the hub.
- Save state (per-plot SRS data) via the existing FastAPI + Neon backend and save-code system — no new infra needed.
- Content (the unit/topic/item catalog) can live as static JSON in the repo, versioned like the other games.

---

## 10. Open Questions

- [x] ~~Draft the full item catalog~~ — done for both subjects (`fren_combined_catalog.json`, 966 items).
- [x] ~~Should FREN151 and FREN152 be one continuous farm?~~ — Yes, confirmed. One continuous sequence, no season split.
- [ ] Audio pronunciation — worth the extra asset effort, or skip for v1?
- [ ] Any interest in this ever becoming a BCM114 Round 2 candidate (story-based games, different SDG), or is this staying purely personal/hobby, separate from all DAs?

---

## 11. Milestone Sequence & Status

| # | Milestone | Content | Status |
|---|-----------|---------|--------|
| 1 | Syllabus content → JSON catalog | FREN151 + FREN152, one continuous sequence (`fren_combined_catalog.json`, 23 weeks / 138 topics / 1047 items) | Done |
| 2 | Plant-state model + SRS scheduling | Per-plot `ease_factor`, `interval_days`, `last_reviewed`, `next_due`, `correct_streak`; growth stages; wilting. Tests: scheduling transitions across sample review sequences | Done |
| 3 | Runtime question generator | Section 5 — varied prompts per `topic_type`, distractors from nearby items, 3-4+ variants per plot. Tests: variant coverage, distractor plausibility, no textbook exercise reproduction | Done |
| 4 | Static farm grid UI | Section 3 stages + wilting, rows = sequence 1-23, chapter labels, no animation required | Done |
| 5 | Save-code backend hook | Shared `shared/save-widget.js` drop-in + `get_state()`/`load_state()` contract | Done |
| 6 | Row-unlock pacing | Section 7 — previous row all at Sprout+ before the next unlocks; FREN151's 11 rows open immediately as a catch-up zone | Done |
| 7 | Polish pass | Section 8 — screenshots-ready visuals, calm "plots needing water" counter, no streak-guilt UI | Done |
| 8 | Grading engine v2 | §14.2 — formalize STRICT/LENIENT tiers, per-item `accepted` array, normalization pass | Done |
| 9 | Report button + `answer_reports` table | §14.2.4 — flag-as-correct queue in the backend, no in-game review UI (see §14.4 decision) | Done |
| 10 | Failure feedback blurb | §14.3 — Phase 1 template version only | Done |
| 11 | Review tab | §14.4 — Random Word Review + Grammar Review | Done |
| 12 | Weekly proficiency tests | §14.5 — one per `sequence` entry, informational only (see §14.4 decision) | Done |
| 13 | Bonus sentence-building sections | §14.6 — 1-2 original sentences per week, tile-drag + translate | Done — 23/23 weeks, 1 original sentence each |
| 14 | Combo bonus + confidence rating | Correct-answer combo speeds SRS growth; self-rated confidence sharpens the wrong-answer penalty | Done |
| 15 | Deliberate interleaving in Review | Stratified round-robin by plot stage in place of a plain shuffle | Done |
| 16 | Adaptive distractor difficulty | Budding+ plots draw harder, similarity-ranked multiple-choice distractors | Done |
| 17 | Cultural notes toggle | Surfaces the cultural-notes half of `fren_supplementary_notes.json`, gated to unlocked rows | Done |
| 18 | Progress dashboard | Row mastery, weakest topics, days-since-last-touch — read-only, no SRS mutation | Done |
| 19 | "Weeds" plot state | Distinct overlay for known French mix-ups (à/a, où/ou, etc.), takes precedence over wilting | Done |
| 20 | Personal error-pattern digest | Classifies why a typed miss happened (mix-up / accent slip / typo / genuine miss); surfaces on the dashboard | Done |
| 21 | Gender-tagging drill | `V_GENDER_TAG` variant tests le/la recall apart from meaning, for unambiguous-article vocab items | Done |
| 22 | Liaison/elision practice | Hand-authored 10-question quiz grounded in the TTS pronunciation watchlist | Done |
| 23 | Achievements (game-local slice) | Plots-automated + full-weeks-automated tiers; needed no new save state | Done — retrofitted into the hub-wide cross-game pattern by Milestone 31 |
| 24 | Mispronunciation-risk report button | Flags known mispronunciation-trap items from the TTS watchlist for human triage | Done |
| 25 | Bugfix: typed-answer inputs weren't clearing between questions | Reset the input's value at every "move on" path (Review/Proficiency/Bonus), not just the main panel | Done |
| 26 | Report buttons extended to Review, Proficiency, and Bonus | Shared payload builders reused across all four practice surfaces | Done |
| 27 | "Greetings & Basics Blitz" arcade minigame | 60-second beat-the-clock vocab match, sequence 1-11 | Done |
| 28 | "Verb Racer" arcade minigame | Grammar-only lane race vs. a fixed-pace rival, sequence 12-15 | Done |
| 29 | "Boutique Dash" arcade minigame | Colour+garment order-rush with a patience clock, sequence 16-18 | Done |
| 30 | "Café Rush" arcade minigame | Food/drink order-rush with a passé-composé confirmation twist every 3rd customer, sequence 19-23 | Done — completes the 4-part arcade family |
| 31 | Achievements retrofit into the hub-wide cross-game pattern | Static `achievements.json` manifest, `achievements_earned` save field, unlock toast, hub-dashboard link | Done |
| 32 | Visual-style switcher | Low-poly / text-based / cartoon / high-def chrome-and-palette themes, desktop-only, `localStorage`-persisted | Done |
| 33 | "What's New" in-game changelog panel | 12-entry curated highlights list (site-wide K16 goal) | Done |
| 34 | L4a: remove the row-unlock pacing gate | Every row unlocked unconditionally from the start; repaired the test suite a session-limit interruption left broken | Done |
| 35 | R2-L1b: practice-progress ledger | Every practice mode (4 minigames, gender drill, liaison, proficiency tests, bonus sentences) feeds one capped, saved ledger shown as a header "Practice score" tile + dashboard section; never touches SRS | Done |
| 36 | V-AB-1: first-run visual-style picker | Four-option dialog shown once when no style is stored (desktop only), skippable to High-def, honest that three styles are CSS chrome | Done |

## 12. Build Notes & Decisions

Decisions taken during the build that the design doc above left open. Appended to as milestones land.

**Milestone 2 — plant-state model + SRS**

- **Plot granularity.** The doc's §2 wording ("vocab words, a conjugation pattern, a grammar rule") is taken literally: vocab/phrase/phonetic get **one plot per item**, a grammar topic is **one plot for the whole rule**, with its example items reserved as fuel for §5's question variants. That yields **722 plots** across the 23 rows (472 of them in FREN151's catch-up zone).
- **Growth stages never regress.** §3 says an incorrect recall carries "no visual punishment beyond *this plot needs water again*", so `plot.stage` is stored and monotonic: a wrong answer reschedules the plot (interval back to 1 day, streak to 0, ease down 0.2) but the plant keeps the highest stage it has reached. Scheduling can get harsher; the picture never does.
- **Time is an explicit in-game day counter,** not the wall clock. `state.current_day` starts at 0 and only moves when the player advances it. That keeps scheduling deterministic and testable, and keeps the game honest about being a study *toy* rather than a nagging daily-streak app.
- **Wilting excludes seeds.** A never-watered plot is always "due" but never "wilting" — there is no plant there yet to droop, and dressing an unplanted plot up as neglect would be exactly the guilt framing §3 rules out.
- **Interval ladder.** 1 day → 3 days → `round(interval × ease)` thereafter, ease starting at 2.5, +0.1 per correct (cap 3.0), −0.2 per incorrect (floor 1.3). Four clean recalls take a plot Seed → Sprout → Budding → Blooming → Automated (interval past the 14-day threshold), which is the pace the doc's §6 example ladder implies.

**Milestone 3 — runtime question generator**

- **13 variants, 3-7 per plot.** Every plot in the farm can produce at least three genuinely different questions and most produce four or five (distribution: 22 plots × 3, 528 × 4, 162 × 5, 8 × 6, 2 × 7 — after the audit-pass change to `blank_target()` noted at the end of this section). Translate-either-direction and type-either-direction cover vocab/phrase/phonetic; grammar adds example translation, word blanks, ending blanks and the pronoun swap.
- **Conjugation pronoun-swapping, given only plain fr/en strings.** The catalog stores no grammatical structure at all — a conjugation table is just six items like `{"fr": "nous parlons", "en": "we speak"}`. So the swap is recovered by parsing: `split_pronoun()` matches the string against the six-person set (longest first, so `il/elle/on` beats `il`, and the elided `j'` matches without a following space) and returns `("nous", "parlons")`. A topic counts as a verb table only if **3+ items split successfully across 2+ distinct pronouns**, and a split is only accepted when the remainder looks like one person's form: **no `/` in it and at most 3 tokens** (room for reflexives, `je me lève`, and compound tenses, `j'ai mangé`). That last rule matters — 10 of the catalog's verb topics pack a whole table, or *two* verbs, into a single string (`"je bois / tu bois / il boit"`), and swapping a pronoun onto those would generate nonsense. Exactly 10 topics qualify as real tables; the other 10 verb topics fall back to translation and word blanks. Swapped questions read `nous _____` with the *other people of the same verb* as distractors, which is both the most plausible distractor set available and self-checking.
- **Ending blanks come from the shared stem.** `parle/parles/parlons/parlez/parlent` share the stem `parl`, so the variant renders `vous parl_____` with `-ez/-ons/-ent/-es` as options. Irregular tables (`ai/as/a/avons/avez/ont`) have no stem of length 2+, so the variant simply isn't offered there — no special-casing needed.
- **What gets blanked.** Function words win when they lead (an article rule *is* about the article), otherwise the rightmost content word is hidden, skipping the catalog's bracketed placeholders (`+ [masc. country]`) and preferring words that occur only once. Where a word does recur, every occurrence becomes a gap, so the answer is never readable off the prompt.
- **Typed answers are lenient on purpose,** and the leniency is a checker concern, not a content one: the displayed answer stays verbatim catalog text, while `check_answer()` folds case and accents, ignores parentheticals, accepts either side of a `/`, and forgives a leading `a/an/the/to`. Mistyping `é` should never cost a plant. The single place that leniency would defeat the point — the phonetic *accents* topic, where the accent is the whole lesson — opts out of French typed variants instead. Later extended, once real play surfaced the gaps: a trailing `+ [placeholder]` template marker (e.g. `"I am + [nationality]"`) is also accepted with the placeholder dropped, since it was never something to type; a bidirectional list of common English contractions (`it's`/`it is`, `don't`/`do not`, and ~30 more pairs, `CONTRACTION_PAIRS`) means either form is always accepted regardless of which one the catalog happens to store; and the Belgian/Swiss French words for 70/80/90 (`septante`/`huitante`/`nonante`, plus `neufante` alongside `nonante` for 90 in case that's genuinely the pairing taught) are accepted as equally correct alternatives to the France-standard compounds (`NUMBER_REGIONALISMS`), scoped to exactly those three catalog items rather than attempting arbitrary compound numbers.
- **Audit pass — `_strip_plus_annotation()`'s trailing-`+`-strip is deliberately left over-lenient for 7 catalog items, not just tightened alongside everything else above.** The function is naive on purpose: `re.sub(r"\s*\+.*$", "", text)` drops from the *first* `+` in the string to the end, which is exactly right for a genuine trailing template slot (`"Je suis + [nationality]"` → `"Je suis"`, `"assez + adjective"` → `"assez"` — the dropped half is only ever a placeholder label, never something a player should have to type) but over-reaches on the 7 items where the catalog uses `+` as mid-string *rule notation* whose tail is the actual lesson, not a slot label: the 4 contraction items in `fren151-w8-grammar005` (`"à + le → au"` → strips to `"à"`, `"à + les → aux"` → `"à"`, `"de + le → du"` → `"de"`, `"de + les → des"` → `"de"`) and the 3 comparative items in `fren152-w4-grammar002` (`"plus + adjective + que"` → `"plus"`, `"moins + adjective + que"` → `"moins"`, `"aussi + adjective + que"` → `"aussi"`). Typed against those specific items, this genuinely over-accepts — e.g. typing just `"à"` would satisfy the full answer `"à + le → au"`. Deliberately not tightened (e.g. by reusing `blank_target()`'s end-anchored `TEMPLATE_SLOT` pattern instead, which would correctly leave these 7 untouched): the explicit brief for this leniency layer is *more* forgiving typed-answer checking, never less, and narrowing the regex to only strip a genuine trailing slot is a strictness change wearing a bug-fix costume. The 7 items are also all offered as multiple-choice variants (`V_EXAMPLE_FR_EN`/`V_EXAMPLE_EN_FR`, exact-match only) alongside their typed ones, so the rule is always tested exactly somewhere in the plot's variant pool even though the typed path is soft on it. Left exactly as-is; flagged here rather than fixed.
- **`blank_target()`'s handling of "/" needed its own fix, separate from the checker leniency above.** *(The single-word half of this was later reversed — see the audit-pass note at the end of this section.)* A masc/fem or similar single-word pair (`"australien / australienne"`) was being blanked as if it were an ordinary sentence — one whole word chosen and hidden, the other left fully visible (`"australien / _____"`, answer `"australienne"` only) — so a player who knew "australien" but not that *this specific* blank wanted the feminine form got marked wrong for the wrong reason. Worse, for items where "/" joins two entirely different **phrasings** rather than one-word variants (`"Comment vas-tu? / Ça va?"`), the same per-word logic could blank a word *inside* one phrasing while the other sat completely intact next to it (`"Comment vas-tu? / Ça _____?"`) — a genuinely nonsensical prompt. Fixed by teaching `blank_target()` to recognise a whitespace-bounded `"/"` (never one embedded in a single token, like the catalog's own `"[places/attractions]"` placeholder) and branch: if every side is exactly one word, blank the *whole* compound as a single unit and keep the answer `"/"-joined` (so the existing slash-splitting leniency accepts either form, and — since this variant is always presented as multiple-choice — the choice buttons now show full pairs like `"italien / italienne"` instead of single gender-committed words); if any side is more than one word, refuse to blank it at all and let the item's direct-translate variants (which already split "/" correctly both ways) carry it instead. A companion test pins the specific plot (`fren151-w6-phrase002-i02`) whose fr text has a bracketed, non-alternation "/" placeholder, confirming it never loses its variant-count floor over this.
- **Audit pass — the single-word "/" branch was making prompts of nothing at all.** Blanking `"australien / australienne"` "as a single unit" sounded right in the abstract, but the alternation *is* the whole item: `re.split` covers every character of the string, so the blanked result was always exactly `"_____"` and nothing else. 93 catalog items were affected (the nationalities and occupations lists, the possessive-adjective table, the irregular-adjective tables), and each one rendered as a bare gap above four full gendered pairs — literally unanswerable except by guessing, and worse than the misfired-blank bug the branch was written to fix. `blank_target()` now refuses **every** whitespace-bounded `"/"` item, which is what the multi-word half of the branch already did, so the two shapes collapse into one rule and the function loses a special case rather than gaining one. The items are still asked, via their translate/typed variants, where `answer_alternatives()` has always accepted either side of a `"/"` — which was the leniency the branch was reaching for anyway. Every plot still clears the 3-variant floor (the distribution shifts as noted above); `test_no_blank_question_is_a_prompt_of_nothing_but_the_gap` walks all 722 plots × every item so a gap can never swallow a whole prompt again.
- **Audit pass — a trailing `+ <slot>` is a template marker, not a word to recall.** `blank_target()` already skipped the catalog's *bracketed* placeholders (`+ [masc. country]`) because `_is_blankable()` rejects `[`/`]`, but four items name the slot as a bare English word — `"assez + adjective"`, `"je n'aime pas + infinitive"`, `"le/la/les plus + adjective"` and its `moins` twin — so the gap landed on the metalanguage and the question became "type the word *adjective*". A `TEMPLATE_SLOT` regex now sets a trailing `+ <slot>` aside before the word is chosen: the slot stays visible in the prompt (`"je _____ pas + infinitive"`) but can never be the answer. It is anchored to the end of the string deliberately — the catalog also uses `+` mid-string as genuine rule notation (`"à + le → au"`), where the word after the `+` is exactly the lesson and still blanks normally.
- **The copyright constraint is a test, not a convention.** `test_every_generated_string_is_recombined_catalog_text` walks all 722 plots × every variant and asserts each answer, choice and note is a substring of some catalog `fr`/`en`/`rule`/`title` string, and a companion test asserts every instruction line comes from this game's own enumerable `INSTRUCTIONS` dict. Nothing a player sees can drift into being workbook material without a test going red.

**Milestone 4 — static farm grid UI**

- **The grid is built once, then only repainted.** 722 cells is far too many to recreate on every answer, so `build_farm()` runs at boot and `render_farm()` afterwards only rewrites each cell's class, sprite and tooltip. Each cell keeps its own click handler (one `create_proxy` per plot) rather than using event delegation — a plot cell *is* the button, and that keeps the fake-DOM tests dispatching straight at the thing a player clicks.
- **Stage sprites, no animation.** 🟤 seed, 🌱 sprout, 🌿 budding, 🌷 blooming, 🌻 automated, with the state carried in a `plot--<stage>` class so CSS does the rest. §8 asked for screenshot-friendly and static, so there are no transitions anywhere: every frame of the farm is a usable screenshot.
- **The sprinkler is a dot, not a second emoji.** A 25px cell cannot carry 🌻 and a sprinkler side by side, so an automated plot gets a small blue drop in its corner via `::after`, and its tooltip says "auto-watered" in words.
- **Wilting is a 6° rotation and some drained colour** — a droop, nothing more. Seeds are excluded, locked rows are excluded, and one watering clears it.
- **The course boundary is invisible by construction.** Row 12 (FREN152 wk 2) is built by exactly the same code path as row 11, with the same classes; only its chapter label reads differently. A test asserts the two rows' classNames are identical so the join can't drift into becoming a visible season break.
- **Every practice question re-rolls, and never twice the same variant in a row.** The plot remembers the last variant it served and passes it to the generator as `exclude`, which is why `open_practice()` keeps a transient `plot.last_variant` — a display concern, so it stays out of the save payload.
- **Warm palette, deliberately unlike the hub's climate games.** Paper and soil rather than dark slate. The hub's rule is that a game's internals are free once you are inside it.

**Milestone 5 — save-code backend hook**

- **The shared widget, unchanged.** One `<script src="../../shared/save-widget.js" data-game-id="champ-de-mots">` line and `window.pyodide = pyodide;` in the boot script; no bespoke bridge, and `shared/save-widget.js` itself is not touched. A test asserts both lines are present in `index.html` so the integration can't silently rot.
- **Only touched plots are saved.** Writing all 722 plots × 6 SRS fields would make every save a ~100KB POST that mostly says "unchanged". `get_state()` writes out only plots that have been reviewed at all, and `load_state()` rebuilds the rest from the catalog — which is where they came from and is versioned in the repo anyway. An untouched farm serialises to under 200 bytes.
- **"Touched" includes plots that only ever went wrong.** Those sit at streak 0 and stage Seed, but they carry a real `last_reviewed` and a reduced ease factor; dropping them would quietly undo the scheduling.
- **Loading resets what the save doesn't mention.** A plot absent from the incoming save is returned to its defaults rather than left standing, so loading a save code never leaves the previous session's plants growing in someone else's farm.
- **Saves are forgiving of drift.** Unknown plot ids (an older catalog revision) are skipped rather than raising, partial plot records fall back to defaults field by field, and an unrecognised stage name falls back to Seed. Any open practice question is closed on load, since it was generated against the farm that just got replaced.

**Milestone 6 — row-unlock pacing**

- **The gate is a growth condition, not a date.** §7 describes FREN152's rows opening "week by week as the semester progresses", but this game has no live "today" (see Milestone 2's day-counter decision), so the rule is implemented purely as *every plot in row N-1 has reached at least Sprout*. A date-driven release would sit on top of that rule rather than replace it, so the gate is ready for one without pretending to have one now.
- **Rows 1-11 are permanently open.** FREN151 is finished in real life, so its 472 plots are a catch-up zone with no gate at all — `CATCH_UP_MAX_SEQUENCE = 11`, which is exactly where FREN152 begins.
- **Sprout, not "visited".** A plot answered only incorrectly is still a Seed, so it still holds the gate shut. The flip side, from §3: because stages never regress, a later wrong answer can never re-lock a row you have already opened.
- **Unlocking is cached.** `render()` asks about lock state once per plot — 722 times a repaint — so the unlocked set is computed once and invalidated on review and on load rather than walked per cell.
- **Locked rows are quiet, not scolding.** Dashed border, faded cells, disabled buttons, and an italic "opens when row N has all sprouted". Locked plots are excluded from the due count and from "water the next plot", so the calm counter never asks for something the farm won't let you do.

**Milestone 7 — polish pass**

- **The counter is a sentence, not a scoreboard.** "472 plots are ready for water today." — with the singular handled — and underneath it a pace note: "Water as many or as few as you like. Nothing here expires, and stopping costs nothing." The empty state neither congratulates nor nags: "Nothing needs water today. The farm is ticking over on its own."
- **A stage tally, not a score.** 🟤 721 · 🌱 0 · 🌿 0 · 🌷 0 · 🌻 1 — how much of the farm is at each stage, with no notion of how far along it "should" be by now. Each open row also carries a quiet "N ready" in its header.
- **The wellbeing constraints are enforced by tests over the shipped files,** not just by care while writing them: no fire emoji in any of `index.html` / `style.css` / `game.py`; the word "streak" never reaches the markup (`correct_streak` stays an internal SRS field); no `setInterval` / `setTimeout` / `datetime` anywhere in `game.py`, because no clock may drive the interface; and no `@keyframes` / `animation:` / `transition:` in the CSS, because §8 asked for static and screenshot-friendly.
- **Enter submits a typed answer,** and plot cells carry an `aria-label` matching their tooltip, so the sprite's meaning is available to a screen reader rather than only to the eye.

**Milestone 8 — grading engine v2**

- **The old two-argument `check_answer(question, given)` is frozen, byte-for-byte.** §14.7's brief was to evolve the existing grading path, and the hard constraint on this milestone was that everything the pre-existing suite checks keeps passing — including the accent-folding-always leniency Milestone 3 shipped. Rather than flip that default and edit old tests to match, the new STRICT/LENIENT machinery is additive: `check_answer` grows `tier=None` and `accent_sensitive=None` keyword arguments, and omitting both reproduces Milestone 3's function exactly (same code path, same result, for every call already in the suite). `normalize_answer` and `answer_alternatives` grow the same kind of optional, default-preserving parameters (`fold_accents=True`, `accepted=None`). The real gameplay path (`submit_answer()`) is the one caller that always passes the new arguments explicitly, so live play gets the actual §14.2 behaviour while every existing test keeps exercising the untouched legacy default.
- **Tier is decided from the answer's own shape, not the item's.** §14.2 says "word count, `topic_type`" — in practice topic_type never has to be consulted separately, because a phonetic/vocab answer that's actually one word already comes out STRICT on word count alone, and the few genuinely multi-word phonetic entries (none currently in the catalog) would correctly fall LENIENT too. `grading_tier(answer_text)` strips the trailing `+ <slot>` marker and any parenthetical first — so a template item like `"I am + [nationality]"` is judged on `"I am"` (LENIENT) rather than the annotated original — then counts whitespace-separated tokens: ≤1 is STRICT, 2+ is LENIENT. A hyphenated number (`"quatre-vingt-dix"`) is one token, so numbers land STRICT exactly as §14.2's table names them, with no special-casing needed.
- **STRICT still gets contraction equivalence and the number-regionalism table.** §14.2's normalization list (shared by both tiers) explicitly includes contraction equivalence, and `NUMBER_REGIONALISMS` is the same kind of thing — a genuine alternate spelling of one exact fact (`nonante` for `quatre-vingt-dix`), not a curated list of *different* acceptable phrasings. "No synonym list" at STRICT is read as: no "/"-splitting, no leading-article drop, no catalog `accepted` array — the LENIENT-only forgivenesses that exist precisely because a phrase can be *said* more than one way.
- **The accent-sensitivity toggle is a real toggle, not just a parameter.** `ACCENT_SENSITIVE` is a module-level global, default `True` (accents matter, per §14.2 — "spelling accents correctly is an assessed skill"), flipped by a checkbox (`#accent-toggle-checkbox`) that calls `on_toggle_accent_sensitivity()`. It's session state, not SRS state, so — same call as `plot.last_variant` in Milestone 4 — it deliberately stays out of `get_state()`/`load_state()`; a fresh page load always starts accent-sensitive. `check_answer`'s own default (`accent_sensitive=None`) still means "fold accents", i.e. the *old* Milestone 3 behaviour, since that's what every pre-existing caller needs; the live game explicitly passes `ACCENT_SENSITIVE` on every submission instead of relying on that default.
- **The `accepted` array is generated lazily, not written into the 966-item catalog file.** §14.2 says "auto-generated at content-build time rather than hand-authored for all 966 items" — the constraint being *no hand-authoring*, not *must be persisted to disk*. Writing a 3-5-phrasing array onto every LENIENT fr/en field across 966 items would meaningfully bloat a catalog that is otherwise pure fact data, for content that's entirely mechanically re-derivable from the item's own text at zero real cost. So `_catalog_item_accepted(item, field)` generates it on the fly via `generate_accepted_variants()` (canonical phrasing, punctuation-stripped, both directions of any contraction, and — only for an explicit `"/"` alternation — each side plus the pair reordered, all mechanically recombined from the item's own text, same copyright rule as §5's question generator) *unless* the catalog item already carries a literal `accepted_fr`/`accepted_en` key, which wins outright. That's the intended growth path from §14.2.4: triaging a report means hand-adding a phrasing to that item's `accepted_fr`/`accepted_en` array in the JSON file, which is then a permanent, human-curated override the lazy generator never touches again for that item.
- **The catalog's illustrative single `"accepted"` key (§14.2's JSON example) became two direction-specific keys, `accepted_fr` and `accepted_en`.** The example shows one grammar record with one `prompt`/`accepted` pair, but every item in this catalog is answerable in either translation direction (`V_FR_EN_TYPED` vs `V_EN_FR_TYPED`), each with its own canonical string — a single undifferentiated `accepted` array would be ambiguous about which direction's phrasings it lists. Splitting it by field is the natural generalization and costs nothing extra to triage by hand later.
- **No UI surface for the `accepted` array itself.** Nothing in the practice panel shows or edits it — per §14.4's decision, reports get triaged by editing the catalog JSON directly, not through an in-game page.

**Milestone 9 — report button + `answer_reports` table**

- **The timestamp is the backend's `created_at`, not something `game.py` produces.** §14.2.4's payload names a `timestamp` field, but the Milestone 7 audit already forbids `datetime`/`time.time` anywhere in `game.py` (no clock may drive the interface — see that milestone's build note and `test_nothing_in_the_game_runs_on_a_timer`). Rather than carve out an exception, the payload sent from Python omits a timestamp entirely and the backend's own `AnswerReport.created_at` (`server_default=func.now()`, same pattern as every other table in `app/models.py`) covers it — the stored row still has exactly the documented shape, just populated on the server side instead of the client.
- **Same Python-computes/JS-sends split as the shared save widget.** `game.py` never does its own `fetch()` — it never has, and `shared/save-widget.js` already established the pattern of Python building state and JS owning the network call. `submit_report()` builds the payload and calls `_dispatch_report()`, which does `from js import window` (deliberately lazy, guarded by `except ImportError`, same defensive style as `_read_catalog_json()`) and calls `window.submitAnswerReport(json.dumps(payload))` if it exists. The actual sender is a small inline `<script>` in `index.html` — not folded into `shared/save-widget.js`, since this is a champ-de-mots-specific feature, not a cross-game one — that does the real `fetch()` to `POST /answer-reports` and swallows any failure (a dropped report is a "let a human know" nicety, not core gameplay, so it fails silently rather than surfacing an error). This also means the fake-DOM test suite never touches the network: `js.window` doesn't exist in the harness, so `_dispatch_report()` is a no-op there and the tests instead pin `_report_payload()`'s shape and `submit_report()`'s one-shot behaviour directly.
- **Report button only for typed answers, matching §14.2.4's own wording** ("every *written-answer* prompt"). A wrong multiple-choice answer already shows the correct choice highlighted with no ambiguity about what was "meant" — there's nothing for a report to add there that the reveal doesn't already say.
- **`marked_correct_answer` is the canonical answer plus whatever the item's `accepted` array already holds**, via the same `_lookup_accepted()` Milestone 8 built — so a report already carries the full context a human would want when triaging it, not just the one string that happened to be on screen.
- **`game_id` on the backend table isn't hardcoded to `"champ-de-mots"`** even though every report from this game sends that value — `AnswerReport` and its endpoints are written so a future game could reuse the same reporting mechanism without a schema change, matching how `Rating`/`Feedback` already key by a `game_slug`/`game_id` string rather than assuming a single game.
- **No in-game admin/review page**, per §14.4's decision — `GET /answer-reports` supports `game_id`/`topic_type`/`item_id` filters for triaging the queue directly (e.g. by pasting a curl/httpie call into a Claude Code session), and that's the whole workflow.

**Milestone 10 — failure feedback blurb (Phase 1)**

- **All three fields are mechanical, none are per-item authored.** §14.3 only explicitly calls "why it matters" a generic per-`topic_type` template, but the same read is applied to all three: "what it is" pulls `plot.rule` verbatim for grammar (exactly as §14.3 names it) and otherwise templates the item's own fr/en pair plus its topic title; "memory tip" is a fixed per-`topic_type` sentence, filled with the item's fr/en where the template calls for it. Nothing here is item-specific insight — that's precisely what Phase 2's (out of scope) Claude API call would add. `test_no_network_or_api_call_anywhere_in_the_blurb_path` pins that Phase 2 hasn't crept in early.
- **Shown on any wrong answer, not just typed ones** — unlike Milestone 9's report button, which §14.2.4 scopes to "every *written-answer* prompt". §14.3 has no such qualifier ("when an answer is marked wrong", full stop), and a wrong multiple-choice pick benefits just as much from "why it matters" context as a wrong typed one, even though it doesn't need the report button's ambiguity-resolution.
- **Lives in its own panel (`#practice-blurb`), separate from the pre-existing `#practice-note`.** `practice-note` already surfaces `plot.rule` unconditionally for grammar plots (shown whether the answer is right or wrong, Milestone 3); the blurb's "what it is" line duplicates that content for a grammar item specifically, but only appears alongside the memory tip and why-it-matters lines on a miss, which is a deliberately different job (post-mistake context, not a standing hint) even where the text overlaps.

**Milestone 11 — Review tab**

- **A "nudge" is a distinct, smaller action from a real watering, on purpose.** §14.4 says a correct Review answer should nudge the interval forward "rather than being untracked" — read narrowly: only the interval and `next_due` move (plus `last_reviewed`, so the nudge survives a save — see below), while `correct_streak`, `ease_factor` and `stage` are left completely alone. Growing a plant's visible stage stays exclusively the daily loop's job (`schedule_after_review()`); if Review could promote a stage too, it would become a second, faster path to "Automated" that quietly undercuts the whole point of spaced, session-gated recall. A wrong Review answer touches nothing at all — no interval reset, no streak zeroing — matching this game's existing no-punishment stance (§3) and keeping Review genuinely low-stakes, since it's explicitly opt-in extra practice, not a second scored track.
- **The nudge sets `last_reviewed` even though it isn't a "real" review**, otherwise it would be silently lost: `get_state()`'s "touched" rule is `last_reviewed is not None or stage != Seed` (Milestone 5), and a nudge changes neither `stage` nor (necessarily) creates any other visible signal for a plot that had never been watered before. Setting `last_reviewed = day` is what makes the nudge actually persist through a save/reload rather than silently evaporating.
- **Both modes reuse `generate_question()` outright** rather than a second generator — §14.4 doesn't ask for new question content, only a new *selection* of which plots to ask about. Grammar Review's "biased toward fill-in-the-blank/conjugation prompts" is implemented as a variant preference at selection time (`_review_variant_for()`): if the plot's own `variants_for()` pool contains `V_BLANK_WORD`/`V_CONJUGATION_SWAP`/`V_BLANK_ENDING`, one of those is rolled; otherwise it falls back to `generate_question()`'s own default random pick across whatever that plot actually offers. This is a bias, not a hard filter — some grammar plots (the ones that aren't conjugation tables and whose examples aren't blankable) never offer those variants at all, and still deserve to show up in Grammar Review with whatever they *do* offer.
- **The count and minimum-stage filter are read from two plain in-page controls** (`#review-count-input`, `#review-min-stage-select`) at the moment a session starts, not stored as separate session-preference globals — there was nothing else that needed to read them, so the DOM element is the single source of truth. Both are defensively clamped/validated (`_review_count_setting()` falls back to the default on anything that doesn't parse as an int, clamped to 1-25; `_review_min_stage_setting()` falls back to "Seed" on any value that isn't a real stage name) so a stray/garbage control value can't crash a session the way an unrecognised save-stage name is already forgiven on load (Milestone 5).
- **No report button or failure blurb inside Review.** Both Milestone 9 and 10 are scoped to the main practice panel; extending either into Review would be a reasonable follow-up but wasn't asked for here, and keeping Review's own panel simpler was the right tradeoff for this pass. Flagging for a later milestone rather than building it speculatively now.
- **Review session state is exactly as ephemeral as `ACCENT_SENSITIVE`** — none of it (`review_mode`, `review_queue`, `review_score`, etc.) reaches `get_state()`/`load_state()`. A save/load mid-Review-session simply drops the in-progress session, which is the same "loading resets what the save doesn't mention" posture Milestone 5 already established for `plot.last_variant`.

**Milestone 12 — weekly proficiency tests**

- **A genuine ambiguity, resolved here: a proficiency test is only offered for an already-unlocked week.** §14.5 says the test covers "every topic in that week regardless of what's currently planted/watered", and §14.4's gating decision says the test itself is purely informational and never gates anything — neither sentence says whether the test is available for a *locked* week. Read literally, "regardless of what's planted/watered" is about SRS state, not row-lock state, and letting a test preview a locked week's full topic list (including topics you haven't even reached in the actual syllabus yet) would work against §7's whole reason for existing — pacing exposure to match the real course, not just pacing *automation*. So `is_proficiency_test_available()` still checks `state.is_row_unlocked()`; a locked row's "Proficiency test" button is present (consistent placement with every other row) but disabled, same visual language as a locked row's disabled plot cells.
- **"Sampling vocab/grammar/phrases" is read as "covering every topic type present," phonetic included.** §14.5's own sentence names three of the catalog's four `topic_type`s, but the same paragraph's opening clause is "covering every topic in that week" without qualification, and only two weeks (both in FREN151's week 1) have phonetic topics at all. Excluding phonetic topics from a week that has them would silently violate "every topic", so `build_proficiency_test()` samples across whatever topic types a week actually has — the omission in the second clause is read as inexact enumeration, not a deliberate carve-out.
- **A grammar topic contributes exactly the same single plot it always has** (Milestone 2's granularity call — one plot per grammar rule, many plots per vocab/phrase/phonetic item), so `_topic_plots()` is a thin wrapper that either looks up that one plot by the topic's own id or walks every item's plot for everything else. This is the same lookup shape `is_conjugation_plot()`/`variants_for()` already rely on elsewhere in the file, not a new pattern.
- **Full topic coverage first, the ~15-20 length second.** `build_proficiency_test()` guarantees one question per topic before doing anything else, then round-robins through the topics again to reach the target length — and if a week ever had more topics than the target length (none do; the largest, sequence 8, has 10), coverage would win outright rather than truncating a topic out. The catalog's smallest week (sequence 22, 2 topics) still produces a short-but-complete test rather than being padded out to a full 15-20 with unrelated content or, worse, silently dropping a topic to hit a round number.
- **Entirely stateless with respect to SRS — more so than Review.** Milestone 11's Review nudges a plot's interval on a correct answer; a proficiency test does neither that nor anything else to `state` — no `review()`, no nudge, nothing — which is what makes "regardless of what's currently planted/watered" and "purely informational" true in the same literal sense for both reading *and* writing: the test doesn't just ignore a plot's current stage when choosing what to ask, it also never writes anything back to it.
- **The entry point is a button on each row's own header** (`row-proficiency-{sequence}`), not a separate flat list of 23 links elsewhere on the page — the row is already the unit of organization for "one per `sequence` entry", so this keeps the mapping between "this week" and "this week's test" visually obvious rather than needing a second index to cross-reference against.

**Milestone 13 — bonus sentence-building sections**

- **Coverage: all 23 weeks, one original sentence each** — the low end of §14.6's "aim for 1-2 per week" rather than authoring two for every week. Given the choice between full breadth at one sentence per week or partial breadth at two, breadth won: every week having *something* to draw on for the review-mode's future extension (or a second pass later) beats several weeks having nothing. Authored by hand, one at a time, reading each week's full topic/item/rule list before drafting so every sentence is actually built from that week's real content rather than generic French — see the sourcing policy below for exactly what "that week's own vocab + grammar" was taken to permit.
- **Sourcing policy (the genuine ambiguity in "using only that week's own vocab + grammar," resolved here):** a bonus sentence's *demonstrated* content — the specific vocabulary being drilled and the grammar pattern being shown off — always comes from that week's own catalog entries (items or `rule` text). Four categories of scaffolding are allowed on top of that, none of which count as "content" being taught by the sentence: (1) subject pronouns and basic `être`/`avoir` conjugation, established from Milestone 2/week 2-3 onward and reused as foundational grammar the same way a real course assumes it from that point on; (2) universal connective words with no dedicated vocab lesson of their own (`et`, `mais`, `puis`, `alors`); (3) correct elision/liaison applied for basic grammatical correctness even in a sentence written before that specific rule's own week (e.g. `j'aime` before week 3's elision lesson) — French orthography doesn't wait for the syllabus to catch up, and refusing to elide would produce *incorrect* French in the name of originality; (4) generic proper names/places as placeholders for a subject or object with no vocabulary content of their own (`Léa`, `Marc`, `Lyon`) — the catalog's own `rule` text already does this (`"un cadeau pour Henri"`). A handful of sentences also lean on a concrete noun from an *earlier* week (`vin` in week 20's sentence, `café` in week 22's) as supporting object vocabulary where that week's own new material is purely grammatical and has no noun of its own to hang the sentence on — always the grammar pattern is that week's own, and never the specific point being drilled.
- **A "tile" is a meaningful chunk, not a strict single orthographic word.** §14.6 says "drag word tiles into correct order," but tiling at the whitespace-token level would split fixed multi-word expressions the catalog itself already teaches as one vocabulary entry — `il y a`, `en face de`, `beaucoup de` are each a single catalog item with a single translation, so splitting `il y a` into `il`/`y`/`a` for independent STRICT translation would ask a player to translate `y` and `a` as isolated fragments, which is nonsense in the same way blanking half of a masc/fem `"/"` pair was (see Milestone 3's audit-pass notes). Tiles are chunked at whatever unit the catalog itself already treats as atomic; the ordering task is still meaningfully non-trivial (4-8 tiles per sentence) and every tile still gets its own translation in task 2.
- **STRICT and LENIENT are fixed by task, not decided by `grading_tier()`.** §14.6 explicitly assigns task 2 STRICT and task 3 LENIENT — a deliberate content decision about which task should be forgiving, not a fact about the answer's own shape the way Milestone 8's auto-tiering is. `submit_bonus_tile_translation()`/`submit_bonus_sentence_translation()` pass `tier=TIER_STRICT`/`tier=TIER_LENIENT` straight to `check_answer()` rather than computing a tier from the text, which also means a short tile gloss doesn't accidentally get LENIENT's `"/"`-splitting/article-dropping/`accepted`-array leniency it was never supposed to have — and a long tile gloss doesn't get penalised STRICT-only treatment grading_tier() would have given it based on word count alone.
- **Tile glosses are stored with trailing punctuation already removed** (`"Hello"` not `"Hello,"`, `"this one"` not `"this one:"`) even though the tile's own French text keeps its comma/colon for display (`"Bonjour,"`, `"celui-ci:"`). `normalize_answer()`'s trailing-punctuation strip covers `. ! ? ¡ ¿ " « »` but not a comma or colon, and STRICT is exact-match-after-normalization — leaving a comma in the expected answer would make a perfectly correct translation fail STRICT purely because the player didn't also type the comma. Stripping it from the *stored answer* (not from `normalize_answer()` itself, which stays exactly as Milestone 8 left it) keeps the fix local to this milestone's own data rather than loosening normalization for everyone.
- **No retry/reshuffle on the ordering task in this pass.** Once every tile is placed, the result (right or wrong) is shown once and the player continues into task 2 regardless — task 2 then walks the tiles in their *real* order, which makes the correct arrangement obvious immediately afterward even after a wrong attempt. A "shuffle and try again" affordance would be a reasonable follow-up but wasn't essential for a first pass, and skipping it keeps `bonus_task`'s state machine to four clean states (`order → translate_tiles → translate_sentence → None`) rather than a fifth "retry" branch.
- **Entirely stateless with respect to SRS and row-unlock, like the proficiency test** — no `state.review()`, no nudge, nothing. Available only for an already-unlocked week (same reasoning as the proficiency test's build note: not gated, but not a way to preview a locked week's content either), entered from a `row-bonus-{sequence}` button on that week's own row header, right next to the proficiency test button.
- **The copyright-safety test for this milestone is structural, not mechanical-recombination.** The runtime question generator's own copyright test (`test_every_generated_string_is_recombined_catalog_text`, Milestone 3) works because *every* string that test walks is mechanically assembled by code with zero manual authorship — it can assert each one is literally a substring of catalog text because nothing else could have produced it. Bonus sentences are the opposite: §14.6 explicitly says "You'll need to actually author these yourself," so they're hand-written prose, and a substring test would either reject legitimate original phrasing (a sentence combining catalog words in a new grammatical arrangement isn't a substring of any single catalog string) or be trivially satisfiable in a way that proves nothing. What *is* tested: `test_bonus_sentence_tiles_reconstruct_the_full_sentence` (the tiles are exactly the sentence, no hidden text), `test_bonus_sentence_is_not_a_verbatim_copy_of_an_existing_catalog_item` (a sentence can't just be one existing item's `fr` text copy-pasted whole), and `test_bonus_sentence_ids_are_unique`. The actual "was every sentence really built from that week's own vocab/grammar" check is the authorial process described above, not a test — the same trust boundary the design doc itself draws by saying a human has to author these.

**Site-wide visual pass — space theme (2026-09-05)**

- **Chrome only, never the farm's own state colours.** This was the site-wide "space theme" visual overhaul (starfield/nebula background, glass panels, gradient buttons, gradient-glow title) already shipped to the hub and SOL, brought here per the same brief. `index.html` gained `shared/space-bg.css` plus the standard `<div class="space-bg">` markup right after `<body>`. `style.css` had its panel/button/border/text colours reworked into the shared violet-glass language — but every plot stage colour (`plot--seed/sprout/budding/blooming/automated/wilting`), `plot--due`, `plot--locked`, and `row--locked`'s dashed/faded treatment kept their exact meaning; only `row--locked`'s hue moved from tan to violet-tinted (still dashed, still faded, still visually distinct — the identity, not the palette, is what's protected). The `.plot`/`.plot--*` CSS block was left byte-for-byte untouched.
- **`.choice--answer` (the correct-answer reveal highlight) kept its green hue but got the same glow treatment as everything else** — read as a state-indicator akin to a meter fill rather than one of the protected stage colours, since it's UI feedback about an answer, not part of the farm's own plant iconography.
- **No meters/progress bars exist in this game to re-skin** (item 4 of the site-wide brief) — all of this game's progress indicators are plain text ("N of M", "83 ready"), not width-based bars, so there was nothing to add a glow to.
- **The Milestone 7 no-animation constraint held throughout** — zero `@keyframes`/`animation:`/`transition:` added; hover states on buttons/choices/tiles use a plain `filter: brightness(1.12)` swap with no eased transition. `test_the_farm_has_no_animation` (which does a literal substring search over the shipped files) caught the wording of this very build note on a first pass — an early draft's own prose contained the literal string "@keyframes", which the test doesn't distinguish from real CSS. Reworded rather than touching the test.
- **270/270 tests green before and after**, verified live via a fresh-port static server: starfield/nebula render behind the glass panels, a plot click opens the practice panel with the new panel/button styling, answering a multiple-choice question correctly still disables the choices and reveals `choice--answer` as expected, and no new browser console errors appeared (the one pre-existing service-worker registration error under this local dev setup is unrelated to this change and appears identically without it).

**Slideshow cross-check merge (2026-09-13)**

- **Source: a slideshow cross-check against the teacher's own lecture slides**, per the "Improvement Ideas" addendum's §1 — the workbook-only text extraction that built the original catalog couldn't catch everything a real lecture covers. Completed for FREN151 (fully) and FREN152 weeks 1–7 plus the week 13 lecture; FREN152 weeks 8–13 (food/drink, partitive, passé composé) have no teacher slides available and remain workbook-only — flagged as warranting a bit more scrutiny than the rest if errors ever surface there.
- **9 new topics, 75 new items merged in**, each inserted into its real sequence week alongside the existing topics there (no topic or item already in the catalog was touched, removed, or reordered): a full "university subjects" vocab set (seq 4), Belgian/Swiss number-word variants (seq 5), the *C'est* vs *Il/Elle est* grammar point plus irregular adjective patterns (-el/-elle, -on/-onne, -eur/-euse, -al/-aux) (seq 10), a physical-build vocab set (seq 11), a "games & instruments" vocab set for *jouer à/de* (seq 13), the "-t-" insertion rule for inverted questions (seq 15), and a full "shopping for clothes" functional phrase set plus an extra materials/sizes vocab set (seq 17).
- **A new `fren_supplementary_notes.json` file** holds two things that don't fit the per-item catalog schema: an 11-entry **TTS pronunciation watchlist** (liaison rules, silent-letter exceptions, the *se lever*-type stem-change accent shift, the "-t-" insertion in inversion, Belgian/Swiss number pronunciation risk, and more, each tagged to the `sequence` it's relevant to) and 5 **cultural notes** (bise/handshake norms, Édith Piaf as a listening extension, letter/email register rules, the *ce/cette* vs *celui/celle* confusion flagged by the tutor). Neither is wired into the game yet — they're the source data for the still-unbuilt TTS and cultural-notes-toggle features from the same addendum (§1, §4).
- **Four merged items needed reshaping, not just insertion, to keep the existing 3-variant-per-plot floor** (`test_every_plot_has_at_least_three_variants` walks every plot in the farm, this one included). The four were each a "menu" of 2-3 distinct phrasings joined by `" / "` and too long to type (over `MAX_TYPED_ANSWER_LENGTH`) — `variants_for()` correctly excludes both the typed variants (too long) and the blank-word variant (`blank_target()`'s deliberate whitespace-bounded-`"/"` refusal, from the Milestone 3 audit pass) for exactly this shape, leaving only the two multiple-choice directions. Rather than special-case the test or leave a plot below the game's own design floor, each was split into its real atomic parts — e.g. `"Je cherche... / Je voudrais... / Vous avez...?"` became three separate items (`"Je cherche..."`, `"Je voudrais..."`, `"Vous avez...?"`), and `"Vous payez comment? par carte / en espèces"` became a question item plus two standalone payment-method items (`"par carte"`, `"en espèces"`). This is a content restructuring, not a leniency change — nothing was dropped, and every resulting item is short enough to independently support typed/blank variants the way the rest of the catalog's atomic items already do. This raised the topic's own item count from 8 to 14, and the farm's grand totals to 138 topics / 1047 items / 790 plots (504 of them in FREN151's catch-up zone, up from 472).
- **284/284 tests green after updating every hardcoded catalog-size assertion** (722→790 plots, 966→1047 items, 129→138 topics, 472→504 catch-up plots) to match the real, larger farm — verified live via a fresh-port static server that the due-counter sentence, stage tally, and a freshly-added plot (`fren151-w4-vocab-slide001-i00`, from the new university-subjects set) all render and answer correctly end-to-end.
- **Not yet built**: everything else in the "Improvement Ideas — Decisions" addendum (TTS practice mode + mispronunciation flagging, gender-tagging drill, liaison/elision practice type, the "weeds" plot state, combo/streak bonus, confidence rating, deliberate interleaving, adaptive distractor difficulty, personal error-pattern digest, a cultural-notes toggle surfacing the notes above, a progress dashboard, and the cross-game achievements system this game would be the first contributor to) — this pass was scoped to the concrete, well-defined content merge only; the feature list is its own, much larger body of work.

**Milestones 14-20 — working through the Improvement Ideas addendum's feature list**

*(Catching up this section's own documentation convention after a run of milestones landed without it — see the note at the end.)*

- **Milestone 14 — combo bonus + confidence rating.** A run of correct answers speeds SRS growth (`combo_bonus`, capped at `MAX_COMBO_BONUS`, applied only to the exponential-growth interval branch, never the two fixed early intervals) and self-rated confidence sharpens the incorrect-answer penalty (`CONFIDENT_WRONG_PENALTY_MULTIPLIER` vs `UNSURE_WRONG_PENALTY_MULTIPLIER`) — a confident wrong answer costs more ease than an unsure one, on the theory that a confident miss is the more informative signal. Both are additive, default-preserving parameters on `schedule_after_review()`/`FarmState.review()`, matching Milestone 8's grading-tier pattern, so every pre-existing call site is unaffected. Both `combo_count` and `current_confidence` are session-only (never saved), same category as `ACCENT_SENSITIVE`. The combo display avoids the words "streak" and any fire emoji per the Milestone 7 wellbeing constraints, and only appears once `combo_count >= 2` so a single correct answer doesn't read as a countdown.
- **Milestone 15 — deliberate interleaving in Review.** `_interleave_by_stage()` replaces a plain shuffle with a stratified round-robin (bucket by `plot.stage`, shuffle within bucket, round-robin across buckets present), guaranteeing a mix of stages rather than trusting chance not to cluster a session on one stage.
- **Milestone 16 — adaptive distractor difficulty.** Once a plot reaches Budding+ (`ADAPTIVE_DISTRACTOR_MIN_STAGE`), `_choice_question()` ranks its distractor pool by spelling/word similarity to the real answer (`difflib.SequenceMatcher`, via `_answer_similarity()`) and samples from the harder end of that ranking rather than the whole pool uniformly — a mature plot gets tested against genuinely plausible near-misses instead of easy distractors. Entirely internal to one function; no changes needed at any of its 7 call sites. `DISTRACTOR_POOL_SIZE = 14` is a hard cap discovered here (an early test threshold assumed a larger pool than `_ordered_pool()` can ever produce).
- **Milestone 17 — cultural notes toggle.** Surfaces the cultural notes half of the slideshow-merge's `fren_supplementary_notes.json` (the TTS watchlist half is still unused, reserved for the eventual TTS feature). Gated to `state.is_row_unlocked()`, matching the existing spoiler-avoidance precedent from proficiency tests and bonus sections — never previews a locked week's content.
- **Milestone 18 — progress dashboard.** A separate, purely informational screen (`dashboard_row_mastery()`, `dashboard_weakest_topics()`, `dashboard_days_since_last_touch()`) — nothing here mutates SRS state or the row-unlock cache. "Weakest" excludes untouched topics (not started yet isn't "weak") and locked rows (same spoiler rule as Milestone 17).
- **Milestone 19 — "weeds" plot state.** A plot flips to a distinct "weeds" overlay, instead of ordinary wilting, when a wrong typed answer matches a specific known French mix-up (`WEED_CONFUSIONS` — à/a, où/ou, ce/se, son/sont, and similar homophone pairs, plus one genuine false-friend, *travailler*/travel) rather than being an arbitrary miss. Takes precedence over wilting in `_plot_classes()`. `is_weed_confusion()` deliberately calls `normalize_answer(..., fold_accents=False)` on both sides — the default `fold_accents=True` would collapse exactly the accent-only pairs (à/a) this feature exists to catch, which is how the bug first shipped and was then caught by test. Persistent (`plot.in_weeds`, added to the save record with a `False` default for old saves) since it's meant to be a durable signal, not a one-session display quirk.
- **Milestone 20 — personal error-pattern digest.** `classify_wrong_typed_answer()` diagnoses *why* a typed miss likely happened — known mix-up (reuses Milestone 19's `is_weed_confusion()`), accent slip (would it have passed with accents folded? reuses `check_answer(accent_sensitive=False)`), close typo (spelling similarity above `CLOSE_TYPO_SIMILARITY_THRESHOLD`, reuses Milestone 16's `_answer_similarity()`), or a genuine miss — and only for typed answers, since a wrong multiple-choice pick carries none of that nuance. Counts persist in a new top-level `error_patterns` save field (empty-dict default, forward-compatible with older saves) and surface as a "Common patterns" section on the Progress Dashboard. Purely diagnostic, same posture as the dashboard itself — never feeds back into grading or scheduling.
- **Milestone 21 — gender-tagging drill.** A new `V_GENDER_TAG` variant tests masc/fem recall apart from meaning, per the addendum's own framing. Eligibility is narrow and mechanical: `gender_tag_parts()` only accepts a vocab item whose own catalog `fr` text leads with an unambiguous `"le "`/`"la "` article (92 of the catalog's items qualify) — an elided `"l'"` genuinely doesn't reveal gender, so those are excluded rather than guessed at, and grammar/phonetic plots are excluded outright (one plot per rule, not per noun; a grammar example sentence starting `"Le ..."` is not a drillable noun). The question shows the bare noun (article stripped) and exactly two choices, `"le"`/`"la"` — a real design departure from every other choice variant's `QUESTION_CHOICE_COUNT`-sized distractor pool, called out as a deliberate, tested exception rather than forcing this into `_choice_question()`'s pool-sampling machinery, which assumes a pool of several plausible-but-wrong *answers* to draw from and would crash sampling 3 items out of a pool of exactly 1. The two choices are the bare articles themselves, never a fabricated `"la tourisme"`-style wrong pairing — gluing the wrong article onto a real noun would be an invented string no catalog item actually contains, where a plain article is just a real French function word already present throughout the catalog, so §5's copyright/recombination rule is satisfied more directly here than by any distractor-pool variant. The English meaning never appears anywhere in the question (prompt, choices, or note) — that's the entire point of testing gender "separately from meaning" rather than alongside it.
- **Milestone 22 — liaison/elision practice ("Liaison practice").** A short, fixed-length multiple-choice quiz (`LIAISON_DRILL_QUESTIONS`, in a new `liaison_drill` key added to `fren_supplementary_notes.json`), entered from its own toolbar toggle button next to Progress Dashboard. Unlike every other practice question in this game, these are **hand-authored, not generated from a plot** — the same posture Milestone 13's bonus sentences already established (some content genuinely needs a human, not a mechanical recombination of catalog facts). Each of the 10 questions is grounded one-for-one in a specific rule from the TTS pronunciation watchlist half of the same supplementary-notes file (liaison before a vowel, silent-final-consonant exceptions, the *se lever*-type accent shift, `-t-` insertion in inversion, Belgian/Swiss number words, and more) — the watchlist's own text is the source, the question/choices/explanation wording is new. One watchlist entry (*travailler*, "commonly mispronounced, worth a manual listen-check") deliberately did **not** become a question — it's a tutor's subjective flag, not a testable mechanical fact, and forcing it into multiple choice would mean inventing a fake rule. Entirely stateless with respect to SRS and row-unlock caching, and gated to unlocked weeks only — same posture and same spoiler-avoidance rule as the proficiency test, bonus sections, cultural notes, and the dashboard. Needed its own feedback wording (`LIAISON_FEEDBACK`) rather than reusing the farm's `FEEDBACK` dict — "this plot is growing" makes no sense for a quiz that never touches a plot, an easy copy-paste mistake this pass explicitly caught and fixed before shipping.
- **Milestone 23 — achievements (a game-local slice only).** The source doc frames achievements as an eventual *cross-game* system this game "would be the first contributor to" — that shared schema/UI is a real design project of its own and stayed explicitly out of scope; what shipped is a game-local reading of the doc's own example milestones ("25/50/100 Automated, full week Automated, etc."). Needs **no new save state at all**: since Milestone 2 already made plot stages monotonic (never regress), "currently Automated" and "ever reached Automated" are the same count, so `automated_plot_count()`/`fully_automated_row_count()` are plain reads of the farm that already exists, not a new tracked stat. Two independent tiers — total plots automated (25/50/100/250/500/750) and full weeks automated (1/5/11/23, the last two being "all of FREN151" and "the whole farm") — each showing every threshold already crossed (most recent first) plus a plain "x of y" progress line for the next one still ahead, so there's always something to see whether or not anything has been earned yet, consistent with this game's no-guilt design stance (§3): a badge is a positive addition, never a missing-badge callout. *(Retrofitted into the cross-game achievements pattern — static `achievements.json` manifest, `achievements_earned` save field, unlock toast, hub-dashboard link — by Milestone 31, below, once that framework existed. Every unlock condition here is unchanged by that retrofit.)*
- **Milestone 24 — a mispronunciation-risk report button, in place of the full TTS practice mode.** Revisited per direct instruction: rather than building the actual audio-playback/mispronunciation-flagging subsystem the addendum originally described (a genuinely separate chunk of engineering — browser TTS, an accent choice, a "flag what I heard" UI), the flagged concern was narrower than that in practice — a handful of catalog items are known mispronunciation traps (per the TTS watchlist), and the useful thing right now is a way to flag them for a human to fix or remove, not to build speech synthesis. `PRONUNCIATION_RISK_NOTES` resolves by exact membership (an item's own fr text against the watchlist's `item` field) rather than a hardcoded list, picking up "travailler" and "du shampooing" automatically — most of the watchlist's 11 entries describe a rule ("liaison consonant sounds," "-t- insertion in inversion") with no single item to attach to, and those stay covered abstractly by Milestone 22's liaison quiz instead. A small risk note shows only for a flagged item, but the report button itself is available on *every* question, flagged or not, and independent of right/wrong or typed/choice — a pronunciation concern is about the catalog text itself, not about how one attempt went, so gating it the way the "should count" report is gated (wrong + typed only) would have missed most of the real use case. Reuses the exact same `answer_reports` backend table and `_dispatch_report()` sender as Milestone 9's report button, with zero schema changes — a fixed marker string stands in for `submitted_answer`, the item's own fr text satisfies `marked_correct_answer`'s "at least one" requirement, and `topic_type="pronunciation"` is enough for a human triaging the queue (`GET /answer-reports?topic_type=pronunciation`) to tell the two report kinds apart. Fixing/removing a flagged item is a manual catalog edit, the same triage workflow every other report type in this game already uses (§14.2.4).
- **Still open from the addendum**: the vaguest item, a family of standalone mini practice games reframing the shelved "Anki export" idea. Everything else that didn't need a scoping conversation first has now been built.
- **Documentation gap, closed here:** Milestones 14-19 landed without their own build notes or milestone tags at the time, breaking this file's own per-milestone convention (see §13). Tags `champ-de-mots-milestone-14` through `-20` were added retroactively against their actual commits, and this consolidated entry covers the notes that should have been written per-milestone. Going forward, each milestone gets its own note at the time it lands, not in a batch afterward.

**Milestone 25 — bugfix: typed-answer inputs weren't clearing between questions.** Reported directly from live play. `open_practice()` (the main farm panel) was the *only* place in the whole file that ever reset `practice-answer-input`'s value — every other "move on" path (`next_review_question()`, `next_proficiency_question()`, `next_bonus_tile()`, and the sentence-boundary reset inside `_begin_bonus_sentence()`, which both `start_bonus_section()` and `next_bonus_sentence()` call) left whatever the player had last typed sitting in the box, forcing a manual delete before the next answer. Fixed by adding the same one-line `_element("...").value = ""` at each of those points — `_begin_bonus_sentence()` clears both `bonus-tile-answer-input` and `bonus-sentence-answer-input` in one place since it's the shared reset point for a fresh sentence regardless of which task ends up running first. No behavioural change beyond that; each fix has a dedicated test (`test_review_tab.py`, `test_proficiency_tests.py`, `test_bonus_sections.py`) that types stale text into the box, advances, and asserts it's empty. 407/407 tests green (400 + 7 new).

**Milestone 26 — report buttons extended to Review, Proficiency, and Bonus.** Also reported directly from live play ("it does not have a report button"). The main practice panel already had both report mechanisms (Milestone 9's "I think this should count" correctness report, Milestone 24's "🔊 Report a pronunciation concern" button); Milestone 11's build note explicitly scoped them *out* of Review at the time ("wasn't asked for here, and keeping Review's own panel simpler was the right tradeoff for this pass") — that call is superseded now that it's been asked for directly, and Proficiency/Bonus never had the conversation at all.
- **Shared payload builders, not four copies of the same logic.** `_report_payload()`'s body became `_typed_wrong_report_payload(question, submitted_answer)` and `_pronunciation_report_payload()`'s became `_plot_pronunciation_report_payload(question)` — both now take an explicit question dict instead of reading `current_question` directly, so Review and Proficiency (whose questions are also `generate_question()` output, carrying the same `plot_id`/`topic_type`/`answer` shape) can reuse them outright. Each mode still gets its own thin `_<mode>_report_payload()` / `submit_<mode>_report()` pair reading from that mode's own result/question globals, matching how `submit_answer()` itself only ever differed from `submit_review_answer()`/`submit_proficiency_answer()` in which globals it touched, never in the grading call underneath.
- **Bonus doesn't share the plot-lookup helpers — it can't.** Bonus sentences are hand-authored (§14.6), not plot-backed, so there's no `plot_id` for `_lookup_accepted()`/`state.plots_by_id` to resolve. Task 2 (tile) and task 3 (sentence) each build their payload directly: `item_id` is `"{sentence_id}-tile-{index}"` or the bare `sentence_id`, and `topic_type` is a fixed marker (`BONUS_TILE_REPORT_TOPIC_TYPE = "bonus_tile"`, `BONUS_SENTENCE_REPORT_TOPIC_TYPE = "bonus_sentence"`) so a human triaging the queue can tell these apart from plot-backed reports — same idea as Milestone 24's `"pronunciation"` topic_type marker. `marked_correct_answer` uses `generate_accepted_variants()` directly on the tile/sentence's own English text (there's no catalog `accepted_en` override to check, since these aren't catalog items), which already includes the raw canonical answer as its first element.
- **Task 1 (tile ordering) gets neither button**, same rule that already keeps a wrong multiple-choice pick report-free everywhere else — it has no typed answer to report on or flag a pronunciation concern about.
- **Scoping matches the original exactly.** The correctness report only appears once a *typed* answer is marked wrong; the pronunciation-concern report is available the whole time a question is open in that mode, regardless of right/wrong or typed/choice (Review's and Proficiency's questions can be multiple-choice; Bonus's tasks 2/3 are always typed by construction). Reset points mirror Milestone 25's input-clearing fix almost exactly: `_advance_review_question()`, `next_proficiency_question()`, and `_begin_bonus_sentence()`/`next_bonus_tile()` are the natural "new question" choke points, so the two report flags and the submitted-answer text reset there rather than being duplicated across every caller.
- **No risk-note display was added to the three extended modes** — only the button. The main panel's `practice-pronunciation-note` (showing `PRONUNCIATION_RISK_NOTES`' text inline) is a nice-to-have surfaced alongside its report button, not part of the report mechanism itself, and wasn't part of what was actually asked for here; the report button alone still lets a player flag any item, watchlisted or not, from any of the three modes.
- New element ids (`review-report-button`, `review-pronunciation-report-button`, `proficiency-report-button`, `proficiency-pronunciation-report-button`, `bonus-tile-report-button`, `bonus-tile-pronunciation-report-button`, `bonus-sentence-report-button`, `bonus-sentence-pronunciation-report-button`) follow the main panel's own naming pattern and are registered in `tests/conftest.py`'s `ELEMENT_IDS`. Tests live in `tests/test_review_proficiency_bonus_reports.py`, mirroring `test_report_button.py`/`test_pronunciation_reports.py`'s coverage (visibility, payload shape, one-shot behaviour, reset-on-next-question) across all four report surfaces. 438/438 tests green (407 + 31 new).

**Also folded into this pass — a genuinely significant production bug fix, made directly to disk mid-session, not authored as part of this milestone's own work:** the real Pyodide browser never defines `__file__` (unlike the pytest harness, which loads `game.py` via `importlib.util.spec_from_file_location` and gets a real one), and `index.html`'s boot script was only ever setting `window.CATALOG_JSON`, never `window.SUPPLEMENTARY_NOTES_JSON` — so `_read_json_asset()`'s filesystem-fallback branch crashed with `NameError: __file__ is not defined` at module-import time on every real page load, ever since the supplementary-notes file was introduced (Milestones 17/22/24's cultural notes, liaison drill, and pronunciation-risk features). Invisible to both pytest (real `__file__`) and this project's own standalone-script verification habit (same loader), so it shipped unnoticed until a live player hit it. Fixed by (1) having `index.html` fetch `fren_supplementary_notes.json` and set `window.SUPPLEMENTARY_NOTES_JSON` before running `game.py`, exactly parallel to the existing `CATALOG_JSON` fetch, and (2) widening `except (ValueError, OSError):` around `SUPPLEMENTARY_NOTES = json.loads(...)` to also catch `NameError`, so a missing global degrades to "no supplementary notes" instead of crashing the whole module if this ever regresses. Verified live in a real browser (the farm now renders — 790 plots, 23 rows — where it previously never loaded at all).

**Milestone 27 — "Greetings & Basics Blitz" arcade minigame, first of four (sequence 1-11).** Direct instruction: build four "extremely gamified" arcade minigames reusing real catalog content, with genuinely different fast-paced interaction patterns than the calm farm loop — timers, scoring, streaks. Blitz is the first: a 60-second beat-the-clock rapid-fire vocab match over FREN151 Ch.1-4.

- **The file-boundary exception, stated once here and not repeated for Milestones 28-30.** The farm's own no-timer/no-animation wellbeing constraint (§3/§8, Milestone 7) is enforced by two tests that do a literal substring scan over two *named files* — `test_nothing_in_the_game_runs_on_a_timer` scans `game.py` for `setInterval`/`setTimeout`/`requestAnimationFrame`/`datetime`/`time.time`; `test_the_farm_has_no_animation` scans `style.css` for `@keyframes`/`animation:`/`transition:`. That constraint exists to protect the *farm's own daily habit loop* from clock-driven pressure — it was never a blanket ban on this game ever having a timer anywhere, the same way "a game's own internals are free once you're inside it" already applies hub-wide. Rather than loosen or reinterpret either test, the entire arcade family lives in two brand-new files the tests don't scan at all: `minigames.py` (imported into `game.py`) and `minigames.css` (linked from `index.html` alongside `style.css`). Confirmed, not assumed: `tests/test_polish.py` was run directly after every change in this milestone and stayed green throughout, unchanged. This is a deliberate, scoped-by-file exception to the parent constraint, not a relaxation of it — the farm's own loop is exactly as timer-free and animation-free as it always was.
- **No circular import, by construction.** `game.py` imports `minigames`, so `minigames.py` never imports `game.py` back — the catalog access and question-generation machinery it reuses (`generate_question()`, `variants_for()`, the live `FarmState`) is handed in once via `minigames.configure(state, generate_question, variants_for)`, called from `game.py`'s own `setup()`, right before `minigames.setup()` wires every DOM listener the minigame family needs entirely on its own. `game.py`'s `render()` and `setup()` each grow exactly one line (`minigames.render()`, `minigames.setup()`); nothing else in `game.py` reaches into `minigames.py`'s internals.
- **Getting the module actually loadable in both the browser and pytest, without relying on `__file__` in the one place it can't be trusted** (see the production bug above): `game.py` tries a plain `import minigames` first, which is what resolves in the real browser, since `index.html`'s boot script now does `pyodide.FS.writeFile("minigames.py", minigamesCode)` (fetched alongside `game.py`'s own source) before `runPythonAsync(code)` — Pyodide's default `sys.path` already includes its working directory, so the normal import machinery just finds it. The `except ImportError` fallback only exists for the pytest harness, where `game.py` is loaded by absolute file path and its own directory isn't necessarily on `sys.path`; that branch inserts it (via `os.path.dirname(os.path.abspath(__file__))`, which *is* trustworthy there) and retries. JS reaches a minigame's own tick function by name via `pyodide.globals.get(...)` (the same lookup `shared/save-widget.js` already uses for `get_state()`/`load_state()`), which only ever sees names bound in `game.py`'s own namespace — so each tick function needs a one-line re-export at the top of `game.py` (`blitz_tick = minigames.blitz_tick`), a name binding rather than a call to any banned timer API, so it doesn't trip the substring scan either.
- **The "timer" is a plain counter, JS-driven, never a Python clock** — `blitz_tick()` decrements `blitz_time_remaining` and is a no-op whenever no run is active, called once a second from a single `setInterval` in `index.html`'s boot script (the same interval will drive Racer/Boutique/Cafe's own tick functions once they land, rather than starting/stopping one per game). Exactly the split-of-responsibility the shared save widget and the answer-report sender already established: Python owns state, JS owns real-world timing.
- **3 lives, not a time penalty — the open design call the brief left for this milestone.** A time penalty would compound the pressure of an already-ticking clock: every wrong answer would shrink the one resource the whole round is measured by, which reads as punishing in exactly the way this game's wellbeing stance (§3) tries to avoid even inside an arcade wrapper. Three lives is a fixed, legible budget a player can see and plan around independent of the clock; running out of lives reads as "that's the round" rather than "the clock just got meaner because you slipped." The brief itself named this as more forgiving and fitting for this game's tone, and the implementation follows that lean.
- **Distractor logic is entirely reused, never reinvented.** Each round restricts `generate_question()` to whichever of its own "translate" choice variants a given plot actually offers (`V_FR_EN_CHOICE`/`V_EN_FR_CHOICE`/the phonetic and grammar-example equivalents) — mirrored in `minigames.py` as plain string constants rather than imported, to avoid the circular import; `test_minigames_variant_constants_match_games_own` pins the duplication against silent drift if `game.py` ever renames one. No typed answers in Blitz at all — rapid-fire and a text input are the wrong shape together, and every other minigame in this family stays choice-only for the same reason.
- **The candidate-plot scan must stay lazy, not eager — a real performance bug caught by the test suite itself, not by inspection.** An earlier draft computed `blitz_available()` (and the candidate-plot pool behind it) eagerly on every `render()` call, which calls `variants_for()` (cold cache) across every one of range 1-11's ~500 plots. Since `render()` runs once during every fresh module's `setup()` — including this game's own `game_env` pytest fixture, on every single test — that turned into a measured ~6x slowdown of the entire suite (roughly 10s to over a minute). Fixed by splitting the check in two: `blitz_available()` now only checks row-unlock state (cheap, and a safe proxy — rows 1-11 always have at least one translate-capable vocab plot in practice), while the real candidate pool is computed once, lazily, and memoized (`_blitz_candidates_cache`) the moment a round actually starts. `tests/conftest.py`'s `_remove_pyodide_fakes()` also grew a fourth name to pop, `"minigames"` — unlike `game.py` (loaded fresh every test via `importlib.util.spec_from_file_location`), a plain `import minigames` statement caches the *same* module object in `sys.modules` across every test in a run, which would otherwise leak one test's farm-specific cached candidate list (holding stale `Plot` objects from a torn-down `FarmState`) into the next.
- **A second, real bug found only by live-browser verification, not by the test suite:** `render_blitz()` was unconditionally destroying and rebuilding the choice buttons (and their Pyodide click proxies) on *every* render — including the once-a-second tick-driven re-renders that only needed to refresh the timer display. In a real browser this meant a player's in-flight click could land on a button that had just been replaced, or a proxy that had just been destroyed (a destroyed `create_proxy` raises if called — see `tests/fakes.py`'s own `FakeProxy` docstring for why that's modeled at all). The pytest suite couldn't catch this: it never drives a real 1-second `setInterval`, so `blitz_tick()` is always called explicitly, one call at a time, with no concurrent click racing it. Fixed by tracking the identity of the last-rendered question (`_blitz_rendered_question`) and only rebuilding the prompt/choices block when the question actually changed; the cheap bits (timer/lives/score/combo text) still update every render. This is the reason the "verify live" instruction mattered here specifically — a fake-DOM test harness has no concept of a race between a click and a timer tick, because it has no real clock at all.
- **No shaming language anywhere in the end-of-round state**, per this game's carried-over wellbeing conventions: running out of lives or time reads as "Time's up!" / "Out of lives for this round" plus the score and this session's best, never "you lost" or "game over." `test_summary_reads_as_a_light_celebratory_readout_not_a_shaming_one` pins a small banned-word list (`fail`, `lost`, `you lose`, `game over`, plus the fire emoji) against the rendered summary text.
- **"Best this session" is deliberately session-only, not saved.** Same category as `ACCENT_SENSITIVE`/`plot.last_variant`/every other piece of transient UI state this game already keeps out of `get_state()`/`load_state()` — a fresh page load always starts at 0, and there was no request for this to persist across visits.
- **Also folded into this milestone (small, self-contained, direct user request): a "Next plot" button inside the main practice panel itself.** Previously, watering several plots in a row meant Close, then scroll back up to the farm-level "water next plot" button, every single time. `on_next_practice_plot()` reuses `state.next_due_plot()` — the exact same selection logic `on_water_next()` already uses — rather than re-deriving "what's next"; its only addition is a graceful fallback `on_water_next()` didn't need for its own farm-level button: if nothing else is due, it closes the panel instead of leaving the button sitting over a stale answered question with a click that would otherwise do nothing. Same visibility gating as the existing report buttons (`answered = current_result is not None`). New element id `practice-next-button`, tests in `tests/test_farm_ui.py`.
- **Verified live in a real browser** (a local static server serving the repo root, the same `hub-dev-server` launch config this hub already uses), not only via the fake-DOM harness: opened the panel, ran a full round start-to-finish, answered both a correct and a wrong translation choice and watched score/lives/combo update accordingly, confirmed the JS-driven `setInterval` → `blitz_tick()` countdown actually advances in real wall-clock time (queried `minigames.blitz_time_remaining` directly via `pyodide.runPython` mid-round), and let the clock run out to see the neutral end-of-round summary and "Play again" button. This live pass is what surfaced both real bugs documented above — the eager-scan slowdown was caught by the test suite first, but the button/proxy race was only visible under an actual ticking timer. 459/459 tests green (438 + 18 for Blitz itself, + 3 for the Next-plot button).

**Milestone 28 — "Verb Racer" arcade minigame, second of four (sequence 12-15).** FREN152 Bridge + Ch.5: numbers/time revision, daily routine, reflexives, comparative/superlative — grammar-heavy, matching the brief's own framing.

- **Grammar plots only, and choice-mode only.** "Pick the correctly conjugated verb form" is read literally: `_racer_candidate_plots()` filters to `plot.topic_type == "grammar"` within the unlocked range, and `_racer_variant_for()` excludes every typed variant (`RACER_TYPED_VARIANTS`) even though a grammar plot can offer one whenever its items are short enough to type — a race needs an instant tap, not a keyboard. The variant preference itself reuses Grammar Review's own bias outright (`RACER_PREFERRED_VARIANTS` mirrors game.py's `GRAMMAR_REVIEW_PREFERRED_VARIANTS`: conjugation-swap/blank-ending/blank-word first, falling back to whatever else the plot offers, same two-tier shape as `_review_variant_for()`) rather than inventing a second bias policy — this is precisely the "reuse the existing conjugation/blank-word question generation" the brief asked for, not a new generator.
- **Discrete steps, not smooth animation — a deliberate visual-treatment call this milestone had to make.** A verb-conjugation question is answered in a few seconds at most, so continuous interpolated motion would spend nearly all its time barely moving between one answer and the next; a clean step per correct answer reads as immediate, legible progress. `minigames.css`'s `.racer-marker` still carries a short CSS `transition` on `left` so a step doesn't visually teleport, but the game logic itself (`_racer_marker_position()`) only ever thinks in whole steps out of `RACER_TOTAL_STEPS = 10` — there is no interpolated position anywhere in `minigames.py`.
- **The rival's pace is a plain tick counter, fixed, never adaptive** — `RACER_RIVAL_TICKS_PER_STEP = 3` (one step every 3 real seconds, via the same shared `setInterval` Blitz's own tick already runs on), matching the brief's explicit "a fixed pace, not adaptive/competitive AI." It advances on `racer_tick()` regardless of anything the player does — no reaction to the player's score, streak, or speed, which is what keeps the rival honest as a fixed benchmark rather than a hidden difficulty slider.
- **A wrong answer doesn't advance the player, and — matching Blitz's own call — a fresh question is rolled either way, right or wrong.** Retrying the identical prompt immediately after a miss would be a weaker test of recall than a new one from the same candidate pool, and it keeps the pacing consistent with Blitz's own "always moving forward" rhythm even on a miss.
- **Both lessons from Blitz's own live-verification were applied here from the start, not rediscovered the hard way:** `racer_available()` stays cheap (row-unlock only, never the full candidate-plot scan) and `_racer_candidate_plots()` is lazy and memoized, exactly Blitz's fix for the eager-scan slowdown; `render_racer()` uses the same last-rendered-question identity check (`_racer_rendered_question`) Blitz needed to stop destroying/rebuilding live choice buttons and their click proxies on every timer tick. Neither bug had to be found twice.
- **No shaming language either way** — a loss reads as "Not this time — the rival crossed first. You reached N of 10," never "you lost" or "game over," matching Blitz's own end-of-round tone; a win reads as "You crossed the line first!"
- **Verified live in a real browser**, same `hub-dev-server` setup as Blitz. Sequence 12-15 isn't the permanent catch-up zone the way Blitz's 1-11 is, so reaching it for a live check meant sprouting rows 1-14 directly via `pyodide.runPython` (`for seq in range(1, 15): for plot in state.row_plots(seq): state.review(plot.plot_id, True)`) rather than clicking through the whole farm by hand — the same forced-unlock technique `tests/test_row_unlock.py` already uses, just run once against the live page instead of the fake-DOM harness. Confirmed: the panel opens with the track and both markers, the rival visibly advances on its own while the player panel is just sitting open (no clicks at all), a correct answer moves the player's marker and rolls a fresh question, and the race ended for real with the rival reaching 10 of 10 first and the exact neutral message above — no console errors throughout. 476/476 tests green (459 + 17 for Racer).
- **An unrelated, pre-existing flaky test was found (not fixed) during this milestone's verification passes**, flagged separately rather than folded in here: `tests/test_review_tab.py::test_grammar_review_is_biased_toward_blank_and_conjugation_variants` samples 20 questions from an unseeded `REVIEW_RNG` and asserts at least half hit the preferred-variant bias, which can occasionally dip below that threshold by chance alone (observed once, 9/20; passed cleanly on every immediate rerun). Nothing to do with the minigame family — it predates this milestone and touches none of the same code — so it's tracked as its own follow-up rather than patched inline here.

**Milestone 29 — "Boutique Dash" arcade minigame, third of four (sequence 16-18).** FREN152 Ch.6: shopping for clothes, colours, demonstratives. A shop-rush game: a customer's order is a genuine colour + garment combination built from real catalog vocab, the player picks the matching item from a small display of options before that customer's patience runs out. 15 customers per shift, one shot each (no retry on a miss), ending in a final tally.

- **Building a correctly-agreed colour+garment combo without inventing any French was the one real design problem here.** A garment item's own fr text already reveals its gender via its article (`"un pull"` is masc, `"une jupe"` is fem); a colour item's fr text already stores both gendered forms side by side (`"noir / noire"`). Picking whichever half matches the garment's article and gluing the two catalog strings together with a space is exactly the same kind of mechanical recombination §5's runtime question generator already relies on everywhere else — the conjugation pronoun-swap is the closest cousin — so the result (`"un sac noir"`, `"une montre beige"`) is real, correctly-agreed French built from nothing but verbatim catalog words.
- **Plural garments and multi-item "/" records are deliberately excluded from the combo pool**, not worked around. The catalog only ever stores a colour's masc/fem *singular* forms, never a plural, so gluing `"noir"` onto `"des chaussettes"` would mean fabricating an unlisted `"noirs"`/`"noires"` out of nothing — exactly the kind of invented text §5 rules out. Same reasoning for a record that packs two real garments into one string via `" / "` (`"un sac à main / un sac à dos"`) — there's no single garment to attach a colour to, so it's skipped rather than gluing a colour onto both halves at once. `test_no_answer_ever_uses_a_plural_garment_or_a_slash_record` pins both exclusions.
- **The garment and colour pools each draw from more than one topic across the full 16-18 range**, not just row 16's own dedicated "Clothing"/"Colours" topics — row 17's "un sac"/"un chapeau" (from its "Materials and accessories" topic) and row 18's accessories (`"une montre"`, `"une crème solaire"`, and similar) are genuine boutique stock too. Row 17's "Extra materials & sizes" topic also quietly holds two more invariable colours (bordeaux, beige) alongside its material/size vocabulary; picking those two out reads the catalog's own `"(invariable colour)"` annotation on exactly those two items' `en` text, rather than a hand-picked id list that would silently go stale if the catalog ever grows another one.
- **Only a correct sale speeds the pace up — a miss changes nothing about the clock.** `BOUTIQUE_PATIENCE_STEP` shaves a second off the *next* customer's starting patience only when the current one is served correctly, floored at `BOUTIQUE_MIN_PATIENCE`; a miss or timeout leaves the pace exactly where it was. This is a direct reading of the brief's own wording ("correct sales... speed up the pace slightly") rather than an invented symmetry.
- **One shot per customer, no retry** — a wrong pick or a timeout immediately rolls the next customer rather than re-presenting the same order, matching the brief's "a miss or timeout loses a customer" framing and Racer's own precedent (a fresh question every attempt, right or wrong).
- **Same lessons from Blitz/Racer's own live-verification applied from the start:** `boutique_available()` stays cheap (row-unlock only), the garment/colour pools are lazy and memoized (`_boutique_garments_cache`/`_boutique_colours_cache`), and `render_boutique()` only rebuilds the option buttons/proxies when the order object's identity actually changes (`_boutique_rendered_order`) rather than on every timer-driven re-render.
- **No shaming language** — a miss or timeout reads as "Not this time — next customer, please." and the end-of-shift tally as "Shift complete! Served N of 15 customers (missed M) · Score: S.", never "you failed" or "game over"; `test_summary_reads_as_neutral_not_shaming` pins a banned-word list against the rendered summary, same posture as Blitz's and Racer's own tests.
- **Verified live in a real browser**, same `hub-dev-server` setup as Blitz/Racer. Sprouted rows 1-22 via `pyodide.runPython` to reach the 16-18 range. Confirmed: the toggle/panel/patience bar all render correctly, a correct pick shows "Sold!" and speeds the bar up, a wrong pick shows the neutral miss message and rolls a new customer, the real `setInterval`-driven `boutique_tick()` visibly counts the patience bar down in wall-clock time (queried `minigames.boutique_patience_remaining` mid-round), and the session-end tally renders with the exact neutral wording above — no console errors throughout. 517/517 tests green (476 + 19 for Boutique Dash + 22 for Café Rush, built in the same pass — see Milestone 30 below).

**Milestone 30 — "Café Rush" arcade minigame, fourth and final of the family (sequence 19-23).** FREN152 Ch.9: food & drink, partitive, passé composé. Same order-rush structure as Boutique Dash (an order, a patience clock, 15 customers, a final tally), themed to food/drink, with one content twist reflecting this range's other grammar focus: every third customer also asks the player to confirm the order in the passé composé — a multiple-choice fill-in-the-blank/conjugation-swap question pulled from a real passé-composé grammar plot, reusing `generate_question()` exactly the way Verb Racer already does rather than building a second question generator.

- **The food/drink pool leans entirely on `_expand_slash_variants()`**, a shared helper written back in the module's original scaffolding (present since Milestone 27/28 landed, unused until now) for exactly this purpose. Catalog items like `"du thé / du café"` → `"tea / coffee"` or `"un poisson / du thon / du saumon"` → `"fish / tuna / salmon"` are really several separate real facts sharing one record, and splitting them gives the order pool genuine variety without inventing a single word. An item whose fr/en sides don't split evenly (`"de la soupe / du potage"` → `"soup"`, two names for one dish) is left alone by that same function, which is exactly right here too — presenting the whole thing as one order is honest about it being one dish with two acceptable names, rather than forcing a split that would silently manufacture a second, fabricated distinct answer.
- **A same-English-meaning distractor is filtered out of the option grid**, not just an identical string. Because a few catalog food items share an `en` translation across genuinely different French names (the soup/potage pair above), a naive distractor sampler could offer two buttons that would *both* be a correct answer to the same displayed English order — a real ambiguity, not a fair wrong choice. `_roll_cafe_order()` skips any candidate whose `en` (case-folded) matches the answer's own before adding it as a distractor.
- **The twist is a fixed cadence (`CAFE_TWIST_INTERVAL = 3`), not a random chance.** A coin-flip "some rounds" would make an already-short 15-customer session's actual twist frequency vary a lot run to run, including runs that never hit one at all. A fixed "every third customer" is simple, testable (`test_twist_cadence_is_fixed_not_random` walks the whole session asserting the exact pattern), and guarantees every full session sees the twist several times, matching the brief's framing of it as a real content addition rather than a rare surprise.
- **The twist's grammar pool is scoped to this range's passé-composé topics specifically** (`CAFE_PASSE_COMPOSE_TOPIC_IDS`: regular avoir-verbs, adverbs-as-time-markers, irregular past participles, être-governed verbs, reflexive passé composé) — not every grammar topic sequence 19-23 teaches (the partitive, the pronouns y/en, futur proche, expressions of quantity all stay out on purpose). The brief names passé composé specifically as the grammar this twist should reflect, and widening the pool to "any grammar plot in range" would dilute that into a generic grammar quiz bolted onto a food game. Not every listed topic id actually ends up eligible — être-governed verbs' own example sentences turn out too irregular for a blank-word or conjugation-swap variant — and that's read from `variants_for()` itself at runtime rather than assumed, the same posture Racer's own `_racer_variant_for()` already takes.
- **A twist round only fully "sells" once both steps are right — the one genuine judgment call this milestone needed.** The brief frames the twist as something the player does *in addition to* picking the right item ("ask the player to also complete... rather than only picking the right item"), not a softer alternative to it, so getting the item right but the passé composé confirmation wrong still counts as a missed customer overall. `test_a_wrong_twist_confirmation_still_counts_as_a_miss` pins this reading; the miss itself stays exactly as neutral as any other ("Not this time"), so the extra step raises the bar without raising the emotional stakes.
- **One shared patience clock covers both stages of a twist round.** `cafe_tick()` doesn't know or care whether the current stage is picking the item or confirming it in the past tense — a timeout at either stage resolves the customer as a miss the same way, which keeps Café Rush's timing model identical to Boutique Dash's single-clock-per-customer design rather than needing a second, twist-specific timer.
- **A correct twist confirmation earns a flat bonus on top of the base sale** (`CAFE_TWIST_BONUS_POINTS = 5` added to `CAFE_BASE_POINTS = 10`, so a twist-completed sale is worth 15 total) — a small, legible reward for the extra step, not a multiplier that would make twist rounds swing the score wildly.
- **Same lessons from Blitz/Racer/Boutique Dash's own live-verification applied from the start:** `cafe_available()` stays cheap (row-unlock only), both the food pool and the twist-plot pool are lazy and memoized, and `render_cafe()` only rebuilds the item-choice buttons or the twist-choice buttons when that particular question's identity actually changes, tracked separately (`_cafe_rendered_order` / `_cafe_rendered_twist`) since the two panels can be mid-transition independently of each other.
- **Verified live in a real browser**, same `hub-dev-server` setup as the rest of the family. Sprouted rows 1-22 to reach the 19-23 range. A full 15-customer session was driven directly via `pyodide.runPython` (calling `submit_cafe_item_choice`/`submit_cafe_twist_choice` with the live module's own current answers, the same "drive the real Pyodide runtime instead of clicking fragile screen coordinates" adjustment made after an early manual-click attempt raced its own re-render): confirmed the twist landed on exactly customers 3/6/9/12/15 as designed, every twist question rendered with real context/prompt/choices in the live DOM (`#cafe-twist-panel` visible only during the twist stage, hidden otherwise), a deliberately-wrong twist answer left `served` unchanged and incremented `missed` while returning to the pick stage for a fresh customer, and the session ended with "Service is over! Served 15 of 15 customers (missed 0) · Score: 175" — 15 sales × 10 base + 5 twist bonuses × 5 = 175, confirming the scoring math live, not just in a test assertion. No console errors throughout.
- **All four arcade minigames are now built.** Blitz (1-11), Racer (12-15), Boutique Dash (16-18), and Café Rush (19-23) span the entire farm in sequence order, each with a genuinely different interaction shape (rapid-fire recall, a lane race, an order-and-patience rush, and an order-and-patience rush with a grammar twist) while sharing one architecture (`minigames.py`/`minigames.css`, the JS-driven tick pattern, lazy/memoized candidate pools, identity-guarded re-renders, row-unlock gating, and the no-shame/no-fire-emoji tone) established across Milestones 27-30. 517/517 tests green (476 before this pair + 19 for Boutique Dash + 22 for Café Rush).
- **A genuine git-concurrency incident during Milestones 29-30, and how the tags were fixed afterward.** Several other games (Canopy, Continuum, Grid, SOL) had their own background sessions committing to this same shared repo at the same time this milestone was being built. Milestone 29's own commit got tangled with a concurrent Canopy commit sharing the git index, and the `champ-de-mots-milestone-29` tag ended up pointing at that Canopy commit (`9734800`, "Canopy: achievements system") rather than anything to do with Le Champ de Mots — confirmed by checking it out and finding zero Boutique Dash content. No actual work was lost — both Milestone 29 (Boutique Dash) and Milestone 30 (Café Rush) landed correctly in the single commit described above (`6d21962`) — but the tag itself was simply wrong. Fixed by deleting the bad tag and re-pointing `champ-de-mots-milestone-29` at the same real commit `champ-de-mots-milestone-30` already correctly pointed to, so both milestone tags now resolve to the one commit that actually contains both, rather than leaving a tag pointing at someone else's unrelated work.

**Milestone 31 — achievements retrofit into the hub-wide cross-game pattern.** Per `planning/ACHIEVEMENTS-SYSTEM-DESIGN.md` (built after Milestone 23, above, shipped) and `planning/TODO.md`'s "roll achievements out everywhere" checklist, which explicitly calls this game out as a **retrofit, not a fresh build**: Milestone 23 already had a real, working achievements slice (two tiers — total plots automated, full syllabus weeks fully automated), but as a game-local system with hardcoded Python dicts, no static manifest, and no hub-dashboard wiring. This milestone reshapes it into the same pattern every other game now uses, without changing a single unlock condition.

- **What moved, and what didn't.** Every threshold (25/50/100/250/500/750 plots; 1/5/11/23 full weeks) and every checker's actual logic (`automated_plot_count()`/`fully_automated_row_count()`, both plain reads of the farm — no new tracked state, since Milestone 2 already made plot stages monotonic) is byte-for-byte the same as Milestone 23. What changed: labels/descriptions now live in a new static `achievements.json` (10 entries, one per threshold, ids `automated_{n}`/`row_{n}`) instead of a hardcoded `ACHIEVEMENT_ROW_LABELS` dict, loaded the same `_read_json_asset()` way the catalog and supplementary-notes files already are (window global first, filesystem fallback — this game's own pre-existing convention, which the design doc's §2 explicitly borrowed back for every other game); a new `achievement_ids_earned()` returns the flat catalog-order id list that's now the cross-game contract's `achievements_earned` save field; a new unlock toast; and a link from the panel to the hub-wide dashboard.
- **The tiered panel shape is kept, not flattened.** The design doc's own §3 documents this as a deliberate judgment call: this game's achievements are naturally tiered (a threshold ladder, not a set of one-shot milestones), so the panel keeps grouping each tier into "earned so far, most recent first, plus the next target" rather than being forced into SOL's flat per-achievement card checklist. `achievements_summary()` keeps its existing `{"automated": {...}, "rows": {...}}` return shape for exactly this reason — it's this game's own panel-facing view, built on top of the same catalog-driven `ACHIEVEMENT_CHECKS`/`ACHIEVEMENT_PROGRESS` dicts (keyed by achievement id) that `achievement_ids_earned()` also reads, so there's one source of truth underneath two different presentations.
- **The toast's auto-hide couldn't reuse the other games' implementation as-is.** SOL/Canopy schedule their toast's hide with a real Python-side timer call. This game's own Milestone 7 wellbeing constraint (`test_nothing_in_the_game_runs_on_a_timer`, a literal substring scan of `game.py`'s source for the browser's clock-driven timer APIs) forbids that literally appearing in this file — the same constraint that already pushed the arcade minigames' real 1-second tick out into `index.html`'s own boot script (Milestone 27). So `show_achievement_toast()` sets the toast's text/visibility directly in Python, then calls a JS-side `window.scheduleAchievementToastHide(ms)` (new, defined in `index.html` next to the existing `submitAnswerReport` sender) to actually schedule the hide — same Python-decides/JS-owns-timing split the report sender and the minigame ticks already established, and a no-op in the pytest harness (no `js.window`) exactly like `_dispatch_report()` already is there. Caught the exact trap CLAUDE.md's own Milestone 27 note already named — an early draft's docstring prose literally contained the banned timer-API name and tripped the substring scan on its own comment, not on real code; reworded rather than touching the test, same resolution Milestone 27 used.
- **The toast CSS also couldn't copy the other games' fade-in/out.** This file's own no-animation constraint (`test_the_farm_has_no_animation`, the equivalent literal scan of `style.css`) rules out an eased opacity/transform transition for the toast. Rather than carve out a file-boundary exception (as the minigame family did for its own CSS), the toast simply appears/disappears instantly via the `.achievement-toast--visible` class and the `hidden` attribute — arguably a better fit for this specific game's static, screenshot-friendly identity than an animated toast would have been anyway. Styled in the same warm-paper, affirmative-green language as `.choice--answer`/the confidence buttons, not copied from the other games' cool violet-glass toast.
- **`achievements_earned` is the only save-payload change**, added to `get_state()` as the write-only projection the design doc's §1 specifies (recomputed fresh every call, never read back in `load_state()`) — a forged/stale value in a loaded save is provably inert (`test_get_state_exposes_achievements_earned_and_never_reads_it_back` loads a save with a fabricated `achievements_earned` list and confirms the live farm's own re-derived value wins outright).
- **Hub-side `script.js` registration (`GAMES_WITH_ACHIEVEMENTS`/`GAME_DISPLAY_NAMES`, slug `champ-de-mots`) is explicitly out of scope for this dispatch** — a shared file other concurrent sessions may be touching, same caveat every other game's own achievements rollout already carried before its own sweep-up pass (see `planning/TODO.md`'s tracking line).
- 533/533 tests green (517 before this milestone + 16 new: catalog sanity, `achievement_ids_earned()` behavior and save round-trip, the tiered summary/panel re-pointed at string ids instead of raw threshold numbers, and the new toast).

**Milestone 32 — visual-style switcher (low-poly / text-based / cartoon / high-def).** Per `planning/TODO.md`'s "Big standalone features" list, unblocked now that the arcade-minigame family (Milestones 27-30) and the achievements retrofit (31) are done. Direct instruction: a switcher chosen on first load and changeable in settings, desktop-only, "helps see what's actually possible visually" + a BCM206 testing-different-options angle.

- **The honest scoping call, made explicit rather than quietly settled for.** Four *full* art styles — a real low-poly 3D mesh, hand-drawn cartoon art, a genuinely higher-fidelity rendered look, each with their own asset pipeline — is not one session's work, and this hub has no build step, no WebGL, no art pipeline to lean on (Continuum's Phase 5 Three.js scene is the one exception elsewhere in the hub, and even that took a dedicated phase). What's real and achievable with CSS/layout alone: four genuinely distinct *chrome-and-palette* treatments of the exact same content — different fonts, borders, shadow language, corner-radius rules and colour palettes, switched live with zero layout or content change. **High-def is deliberately just this game's pre-existing look** (the site-wide glossy violet-glass theme, unchanged) kept as the default baseline, not a fifth invented palette dressed up as "more high-def than the others" — the existing look is already the most polished/rendered-looking of the four, and manufacturing a *sixth* look just to call it the "true" high-def would be exactly the fake claim this task's own brief warned against. **Text-based is the one built with the most genuine confidence** — dense, monospace, minimal-chrome is the closest thing CSS alone can do convincingly, and it's the only style that also reskins the farm grid's actual sprite glyphs (see below), not just the chrome around it. Low-poly and cartoon are real, distinct, honestly-labeled CSS treatments (flat faceted colour-blocks; bright candy pill-shapes with thick borders and hard drop shadows) — good and clearly different from each other and from the baseline, but not claimed to be anything they're not.
- **Two new files, not a rewrite of the existing stylesheet.** `style.css` has no CSS custom properties for colour at all (every value is a literal hex/rgba), so "swap a `:root` variable set per theme" wasn't actually available without a full 1400-line refactor — out of proportion to what was asked. Instead, `visual-styles.css` (loaded after `style.css`/`minigames.css`, so its cascade wins) carries three `html[data-visual-style="..."] ...` override blocks (lowpoly/textbased/cartoon); the fourth, highdef, needs zero rules of its own since it *is* the untouched baseline. `visual-style.js` is a small, dependency-free IIFE that sets the `data-visual-style` attribute, persists the choice to `localStorage`, and wires the settings buttons — same architecture `games/aftermath/settings.js` already established for this hub (independent of Pyodide entirely, wired up before `game.py`'s own boot script runs, so the control works even if Pyodide never finishes loading). Same category as this game's own `ACCENT_SENSITIVE` toggle: a browser-level UI preference, never written into `get_state()`'s save payload, since a save code is meant to be portable across devices/browsers and a local visual preference shouldn't silently follow it.
- **The farm's own plot-stage colour meanings are untouched at the source, same rule the site-wide space-theme pass already established** (that pass's own note: "the identity, not the palette, is protected"). Not a single hex value in style.css's `.plot`/`.plot--*` block was edited — `test_visual_styles_css_never_touches_plot_stage_color_source` pins two of those original literal colour values still present, unedited, in `style.css`. Where a style wants the farm grid to look different at all, it layers a `filter` on top from the separate, later-loaded `visual-styles.css` file instead (saturate/contrast for low-poly and cartoon, grayscale+contrast for text-based) — every stage stays exactly as distinguishable from every other stage as it always was, just tinted, and the underlying rule that actually encodes the meaning is never touched.
- **Text-based additionally re-letters the farm grid itself**, the one place a style goes beyond chrome: each `.plot` cell's emoji glyph is hidden (`font-size: 0`) and a plain monospace letter is shown instead via `::before` (`.`/`s`/`b`/`B`/`A` for Seed/Sprout/Budding/Blooming/Automated), which needs its own explicit `font-size` since pseudo-elements inherit the *computed* font-size of their originating box. Every cell's existing `aria-label` (Milestone 7) is completely unaffected, so this loses nothing for assistive tech — only the visible glyph changes. The pre-existing `.plot--automated::after`/`.plot--weeds::after` corner-dot markers (style.css, untouched) keep rendering on top exactly as before, since those are a different pseudo-element (`::after`, not `::before`).
- **Icon labels get an ASCII-equivalent swap under text-based**, reusing style.css's own "Icon labels" pure-CSS `::before content` technique rather than touching any markup: 💧→`>`, 📖→`#`, 🚩→`!`, 🔁→`~`, 📝→`*`, 🧩→`+`, 🔒→`x`. Cosmetic only, no behavioural change.
- **"Chosen on first load" is read as an automatic sensible default, not a forced picker dialog.** A first-ever visit has no `localStorage` entry yet, so `visual-style.js` applies High-def (this game's existing look) immediately and silently; the Settings panel is where it's changed afterward. A modal "pick your style before you can play" screen would be one more thing between opening the game and actually playing it, for a purely cosmetic choice — not what the brief's own phrasing ("chosen on first load *and* changeable in settings") was asking to gate anything on.
- **Desktop-only, as explicitly scoped** — no mobile-specific layout work was done for the new settings control or any of the three new themes; the existing `@media (max-width: 600px)`/`(max-width: 640px)` rules elsewhere in `style.css` are untouched and unextended.
- **No settings-panel markup existed in this game before now** (unlike `games/aftermath/index.html`, which already had a general `#settings-panel`), so this milestone adds one from scratch, scoped to exactly the one control asked for — a natural home for any future per-game setting, but nothing beyond the visual-style switcher was built into it speculatively.
- **Verified live in a real browser** (the shared `hub-dev-server` static server), not only via the fake-DOM harness, since this is a purely visual/JS-preference feature with no Python function underneath most of it: opened Settings, switched through all three alternate styles plus back to High-def, confirmed each one changes the toolbar/panels/farm grid/practice panel/choice buttons/plot glyphs distinctly, answered a practice question under Cartoon to confirm the correct-answer reveal and disabled-choice states still render correctly under the new palette, confirmed the choice persists across a fresh page load (localStorage), and confirmed High-def is pixel-for-pixel the pre-existing look. Zero new browser console errors under any style (the only console errors present are a pre-existing AdSense placeholder-client-ID iframe-framing warning under this local dev setup, identical with or without this feature, same category of pre-existing noise CLAUDE.md's "Site-wide visual pass" build note already flagged for the service-worker registration error).
- 542/542 tests green (533 before this milestone + 9 new, `tests/test_visual_style_switcher.py` — file/link wiring, all four style ids present in both the settings markup and `visual-style.js`, `localStorage`-only persistence with no save-state leakage, the plot-colour-source protection check above, and this file's usual no-motion-CSS/no-timer scan applied to the two new files since `test_polish.py`'s own scan only covers `style.css`/`game.py` by name).

**Colorblind-safety audit (site-wide goal, `planning/TODO.md`, origin A10 reinforced by L5).** Investigate-first, per the Okabe-Ito/redundant-coding method Continuum's Phase 5, Canopy's B9, and Grid/Drift's own trend-graph fixes already established. Walked `game.py`/`style.css`/`minigames.py`/`minigames.css`/`visual-styles.css` for every place game-state meaning rides on colour alone, with particular attention to the four-theme visual-style switcher (Milestone 32) potentially having introduced a colour-only cue the others don't share.

- **Plot stages (Seed/Sprout/Budding/Blooming/Automated) are not a hue-only encoding to begin with** — each stage has carried its own emoji sprite (🟤🌱🌿🌷🌻) plus a full tooltip/`aria-label` sentence since Milestone 4, so this is closer to Canopy's B9 shape (a progression, not a discrete red/green pair) except already redundantly coded by icon from day one, nothing further needed.
- **The automated-sprinkler dot (`::after`, blue) vs. the weeds mix-up dot (`::after`, olive/amber) — the one place two plot-overlay colours could plausibly be confused — are already told apart by position, not hue**: `game.py`'s own `_plot_classes()`/CLAUDE.md build note places them on opposite corners (top-right vs. bottom-left) specifically so they can coexist and stay distinguishable, and each has its own explicit tooltip/`aria-label` text (`AUTOMATED_TOOLTIP_NOTE`/`WEEDS_TOOLTIP_NOTE`) via `_plot_title()`. Wilting (`filter: grayscale(0.55)` + a 6° rotation) and locked (`opacity`+`grayscale(1)`) aren't colour-pairs at all.
- **`.choice--answer` (the correct-answer reveal) is a single highlight colour with no paired "wrong" colour to confuse it with** — a wrong pick simply stays the default `.choice` styling (confirmed in `game.py`/`minigames.py`: `button.className` is only ever set to `"choice"` or `"choice choice--answer"`, never a third "wrong" class). `FEEDBACK`'s correct/incorrect text is rendered in the same single `.practice-feedback` colour either way and is distinguished entirely by wording ("Yes — …" vs. "Not quite — …"), so there's no colour discrimination task at all — the same "single-state, not a paired good/bad colour" shape SOL's and Aftermath's own audits already found safe.
- **The confidence-rating buttons ("Sure"/"Unsure") and the gender-tagging drill's "le"/"la" choices carry no colour-by-value coding** — confidence reuses one shared `.selected` highlight for whichever button is picked (distinguished by its own label text, not colour), and the gender buttons reuse plain `.choice` styling with no colour tied to gender.
- **Boutique Dash's and Café Rush's shop-rush patience bars (`minigames.css`'s `.shop-rush-patience-fill`, green→gold, swapping to gold→orange-red under `--low`) are a single continuous meter, not a two-state pair a player has to tell apart** — and its primary legible signal is the bar's own shrinking *width* (`fill.style.width` in `minigames.py`), which reads perfectly well with no colour perception at all, the same "arc/bar length is its own redundant cue" reasoning Herd's coupling-dial audit already applied to a comparable green-to-red gradient meter. The arcade minigames' urgency-pulsing timer stat (`.minigame-stat--timer`/`--urgent`, amber→orange-red) is always paired with the literal countdown text itself (`blitz-time-display`'s `f"{blitz_time_remaining}s"`), so the colour there is decorative reinforcement, not the signal.
- **Verb Racer's player (🚗) vs. rival (🚙) markers** are distinguished by glyph shape, by lane (separate rows on the track), and by an additional `grayscale` filter on the rival marker — three non-colour cues stacked on what was never actually a hue-only distinction.
- **The progress dashboard and error-pattern digest are plain text throughout** (`dashboard-mastery-row`/`dashboard-weakest-topic`/`dashboard-pattern-row` all share one plain text colour, confirmed by grep against `style.css`) — unlike Grid's/Drift's own SVG trend graphs, this game has no chart/graph/sparkline anywhere to carry a colour-only line-identity problem.
- **Checked all four visual styles (`visual-styles.css`) specifically for a style introducing a colour-only cue the others don't have** — none do. Text-based hides the plot's emoji sprite and shows a monospace letter via `::before` instead, but the automated/weeds `::after` corner dots (already position-coded, not touched by this file) keep rendering underneath text-based's blanket `.plot` grayscale filter exactly as before — since their distinction never depended on hue, greying them out changes nothing about their legibility. None of the three override blocks (lowpoly/textbased/cartoon) touch `.shop-rush-patience-fill`, `.minigame-stat--timer`, or the racer markers at all, so those stay identically (already-safe) coloured across every style.
- **Conclusion: audited, no change needed.** Every colour-carrying state in this game already has a non-colour cue doing the real work (an icon, a corner position, a shape, a bar's own length, or plain text), matching the "audited, no change needed" precedent SOL/Tide/Aftermath/Herd/Thaw/Loop/Trade Empire's own audits already set, rather than inventing a fix where none was needed. 542/542 tests unaffected (no code change made); confirmed live-verification wasn't required since nothing visual was touched.

**Onboarding-tooltip coverage check (site-wide goal, `planning/TODO.md`, origin A14).** Investigate-first, same method every other game's own pass in this series used: read the Tutorial's 10 steps and the How-to-Play read-through (auto-generated from the same list by `shared/tutorial.js`), then walked every permanent toolbar button, farm-grid cell, and all four arcade minigame panels against "a returning player who's forgotten the tutorial."

- **Conclusion: audited, no change needed.** This game already sits well above the bar the other 11 games' own passes found gaps in, for a concrete reason — its build history already ran a heavier "explain the mechanic in place" discipline than most other games (§14's grading/review addendum explicitly designed the accent toggle, confidence rating, and typed-answer strictness around a visible `<details class="info-toggle">` disclosure each, years before this site-wide check existed).
- **Every toolbar button already carries a real emoji+text label** (🎓 Tutorial, 📖 How to Play, 📊 Progress dashboard, 🗣️ Liaison practice, 🏆 Achievements, and all four arcade minigame toggles by name) — nothing icon-only anywhere in `.game-toolbar`.
- **Every farm-grid plot cell already carries a full sentence via both `title` and `aria-label`** (`_plot_title()`, `game.py`), unconditionally, for all 790 plots — label, topic, growth stage, and (when applicable) "auto-watered," a weeds note, or "ready for water." Confirmed live: a fresh Seed cell's tooltip reads `"A — L'alphabet · Seed · ready for water"`.
- **Every non-obvious mechanic already has a standing `<details class="info-toggle">` disclosure** right next to it, not hidden behind a hover: accent-sensitivity, confidence rating, typed-answer STRICT/LENIENT grading, and Review's "nudge vs. grow" distinction. This is the exact "thorough `<details class="info-toggle">` disclosures already exist on every non-obvious panel" shape this session's Trade Empire audit found sufficient.
- **All four arcade minigames were checked specifically, per the task's own call-out.** Each panel opens with a permanent, always-visible paragraph stating its full rules (timer/lives for Blitz, the fixed rival pace and "first to the line" for Racer, the patience clock and per-sale speed-up for Boutique Dash/Café Rush, and Café Rush's passé-composé twist cadence) — nothing here depends on remembering the tutorial. The two spots that looked like they might be gaps on first pass turned out already covered elsewhere in the same permanent panel: Blitz's heart icons (`❤❤❤`, no separate label) are already glossed by the panel's own opening line ("60 seconds, 3 lives"), and Racer's 🚗/🚙 markers are already disambiguated by the permanent `"You: X of 10 · Rival: Y of 10"` status line directly under the track.
- **The visual-style switcher (Milestone 32) needed no changes and nothing new to verify across styles**, since nothing was added — spot-checked live anyway (High-def → Text-based) to confirm the audit's "already self-explanatory" reading holds under a different theme too: the settings panel, toolbar, and farm status readouts all render with the same wording, just restyled, and no new console errors appeared.
- **Verified live** against the shared `hub-dev-server`, in a dedicated tab: read the rendered page and a plot cell's live `title`/`aria-label` via direct DOM inspection (matching the source-level reading above), confirmed zero console errors, and switched the visual style to Text-based to confirm the same conclusion holds under a non-default theme. Full pytest suite: 542/542 passing, unchanged (no code was modified — this pass concluded no gap existed to fix).

**Milestone 33 — "What's New" in-game changelog panel (site-wide goal, `planning/TODO.md`, origin K16).** The last of the 12 hub games needed for this goal; resumed a prior background session that hit an infrastructure rate limit mid-wire — the `Promise.all` fetch, `window.CHANGELOG_JSON`, and `game.py`'s `_read_json_asset()`-backed `CHANGELOG`/`render_changelog()`/`on_toggle_changelog()` were all already in place and correct, matching the exact loading contract `ACHIEVEMENTS` already established (Milestone 31).

- **The changelog is a curated highlights list, not all 32 milestones verbatim** — 12 entries (within the 10-15 range asked for, given this is the largest game on the site by milestone count), sourced from this file's own milestone history and dated against `git log --follow -- games/champ-de-mots/CLAUDE.md`. Several individually-numbered milestones (14-24's study-quality feature run; the four arcade minigames) are folded into one entry each rather than one line per milestone, matching how Continuum's own K16 entry groups by era/phase rather than by numbered milestone.
- **Tests follow this file's own `test_achievements.py` shape, not another game's card-shaped changelog test file.** `render_changelog()` renders a flat one-`<p class="changelog-row">`-per-entry list (reusing the Progress Dashboard's own row idiom, per the code's own build comment), not a two-part date/text card the way some other hub games' changelog panels render — so `tests/test_changelog.py`'s panel-content assertions read `panel.children` as single `<p>` rows (`"{date} — {entry}"`, split on the em-dash) rather than indexing into a card's two children.
- **A real legibility bug found and fixed, not merely checked for** — this is the one game on the site with a visual-style switcher (Milestone 32), so the task's own instruction was to verify the new panel across all four styles, not just the default. `.changelog-row` inherited `.dashboard-panel`'s `#33291f` dark-brown text colour (tuned for the default High-def theme's warm-cream `#fbf7ee` panel background) against Low-poly/Text-based/Cartoon's own dark `.dashboard-panel` overrides (`#22323d`/`#0a0f12`/`#3a1868`), measuring a ~1.02:1 contrast ratio under Cartoon — visually confirmed as near-invisible, not just computed. Fixed with one `.changelog-row` colour override per alternate theme in `visual-styles.css`, each reusing that theme's own already-established light body-text colour (`#eef3f6`/`#b9f5c9`/`#f5eaff` respectively — the same colours `.secondary`/`.primary`/`.practice-input` already use against the same backgrounds) rather than inventing new ones, keeping the "one shared palette per theme" discipline Milestone 32 itself established. **The identical pre-existing bug already sits on the Progress Dashboard's own rows** (`.dashboard-mastery-row`/`.dashboard-weakest-topic`/`.dashboard-pattern-row`/`.dashboard-since`, same `#33291f`, same missing override) — not introduced by this milestone, and left unfixed here since it's out of the changelog panel's own scope; flagged as a separate follow-up.
- **Verifying the fix live required two separate cache-staleness workarounds, not just one.** Beyond the already-documented `sw.js` stale-while-revalidate trap (unregister + clear Cache Storage), a plain top-level navigation with a cache-busting query string on the page URL was not enough on its own to pick up the `visual-styles.css` edit — the browser's own HTTP disk cache served a stale copy of that *linked* stylesheet specifically (confirmed via `document.styleSheets`' live `cssRules`, which still lacked the new rule after a fresh navigation and even after an automated "hard reload" keypress), independent of both the service worker and of a manual `fetch(..., {cache: 'no-store'})` sanity check (which correctly showed the fresh file, since it bypasses the cache the `<link>` tag itself doesn't). Resolved by temporarily cache-busting the stylesheet's own `<link href>` for verification, then reverting that temporary query string before committing — worth remembering for the next session that edits a CSS file this dev server serves without cache-control headers.
- **Verified live** against the shared `hub-dev-server`: the panel opens with all 12 real entries, newest-first, across High-def, Low-poly, Text-based, and Cartoon, with zero new console errors under any of them (the only console noise present, with or without this change, is the pre-existing AdSense placeholder-client-ID failure this file has flagged before).
- 553/553 tests green (542 + 11 new in `tests/test_changelog.py`). This closes the site-wide K16 goal: all 12 hub games now have their own changelog panel.

**UI decluttering pass (2026-09-20, `planning/TODO.md` closing task — audited, no change needed).** Investigate-first, same method as the colorblind-safety and onboarding-tooltip passes above: read `index.html`/`style.css` top to bottom against the user's own crowding note ("everything looks very crowded... making sections either collapsible or other 'screens' within a game could help") and the established `<details>`/toggle-panel fix pattern (Tide's `.ticker-history-toggle`, this game's own `.info-toggle` badges), then cross-checked the verdict against Loop's and Thaw's own audits from the same pass (`games/loop/CLAUDE.md` found and fixed a real "flat run-on stacking" issue in its `#status` tail; `games/thaw/CLAUDE.md` found none) to calibrate what "genuinely crowded" means in practice before deciding this game's case either way.

- **Every one of the task's own "usual suspect" panels is already screen-separated.** The Progress Dashboard, Achievements panel, Settings panel, "What's New" changelog panel, and How-to-Play are each a `<div class="dashboard-panel" hidden>`/`<div class="section" hidden>` toggled by its own toolbar button (`#dashboard-toggle-button`, `#achievements-toggle-button`, `#settings-toggle-button`, `#changelog-toggle-button`, `#howto-toggle-button`) — none render by default. The error-pattern digest (Milestone 20's "Common patterns" section) lives inside the already-collapsed dashboard panel, not loose on the always-visible farm screen. Cultural notes are likewise a `#cultural-notes-toggle-button` → `#cultural-notes-panel[hidden]` pair with its own `.info-toggle` explaining what it is. Review's own controls (`#review-controls`) start `hidden` until `#review-toggle-button` is pressed.
- **`#status`, the one block always visible every round, is already grouped rather than a flat stack.** The four headline numbers (day, due count, progress, unlocked rows) live in `.farm-dashboard`, a `grid-template-columns: repeat(auto-fit, minmax(150px, 1fr))` layout of `.stat-tile` cards, each with its own icon (`📅`/`💧`/`🌱`/`🔓`) via `::before` — visually chunked, not four bare `<p>` lines run together. Below that grid sit exactly two supplementary status lines (`pace-display`, `stage-summary-display`), the watering controls, and the accent-sensitivity toggle with its own `.info-toggle`. That's a materially shorter always-visible stack than Loop's or Thaw's own `#status` blocks (a dozen-plus lines each), so the "flat run of many plain lines with no header or break" shape that made Loop's tail end a real offender doesn't reproduce here.
- **Every non-obvious mechanic on the main screen already sits behind its own `<details class="info-toggle">`** — confirmed by the earlier onboarding-tooltip pass and re-confirmed here: accent sensitivity, confidence rating, typed-answer grading, the "should count" report button, the pronunciation report button, and both legend footnotes (wilting, row-unlocking) each already collapse their own explanatory paragraph. Nothing on-screen is left unwrapped that the site's own disclosure idiom would normally wrap.
- **`#practice-panel` (and the Review/Proficiency/Bonus panels it mirrors) is a contextual sequence, not a stacked pile of supplementary panels** — every element inside it (prompt, choices/input, confidence buttons, feedback, report buttons) is either hidden until the right moment in that single practice interaction or directly load-bearing for answering the current question, the same "checked every round, not supplementary" reasoning Thaw's own audit used for its per-turn readouts. Collapsing any of it behind a `<details>` would add a click to the one interaction the whole game is built around.
- **The eleven-button `.game-toolbar` was considered and left alone.** It carries no CSS of its own in this game's `style.css` — buttons simply wrap like the same unstyled toolbar `<div>` Tide/Aftermath/Continuum and every other multi-panel game on the site already use — so a long toolbar here is the site's standard idiom appearing with more buttons because this happens to be the site's largest game by feature count, not a champ-de-mots-specific crowding problem. Restyling or regrouping the toolbar pattern itself would be a site-wide chrome decision, out of scope for a single-game pass.

**Conclusion: audited, no crowding problem found that isn't already solved by an existing toggle panel or `.info-toggle` disclosure.** No `index.html`/`style.css`/`game.py` change made. Full pytest suite (553/553, unchanged) stays green since nothing was touched. Live-browser verification wasn't reachable this session — the shared dev-server's browser pane was mid-use by another concurrent session actively navigating between games (Drift/Loop/SOL tabs observed live mid-audit), the same contention Loop's own pass from this series hit — so the verdict rests on the same code-level reading (`index.html`/`style.css`/`game.py`) that grounded the colorblind-safety and onboarding-tooltip passes above, cross-checked against Loop's and Thaw's own already-live-verified audits of the identical concern.

**Milestone 34 — L4a: remove the row-unlock pacing gate, and repair the test suite a session-limit interruption left behind.** `planning/TODO.md`'s L4a item asked for exactly this: "Remove the row-unlock pacing gate entirely — unlock every row immediately, so a player joining weeks into the course isn't locked out." A prior background session had already made the real, correct code change and hit an account-wide rate limit before it could finish the job — this milestone is the finishing pass, not a redesign.

- **What L4a actually changed, and why.** Milestone 6's original design (§7) opened a row only once every plot in the row before it had reached at least Sprout, with FREN151's 11 rows marked open immediately as a "catch-up zone" since that course was already finished in real life. That pacing model assumes a player who starts on day one and moves through the syllabus roughly in step with it. It breaks down for anyone joining after the fact — a player picking this up partway through a real FREN152 semester would find weeks their classmates have already covered sitting locked behind a gate that has nothing to do with what they personally know, which defeats the entire point of a study tool built to let you drill *your* syllabus. `FarmState.is_row_unlocked()` (game.py, ~line 415) now returns `True` unconditionally for every row that exists, and `_compute_unlocked()` (~line 438) is a one-line "every row's sequence, no gating" set comprehension. `CATCH_UP_MAX_SEQUENCE = 11` is kept, but purely as the content-boundary fact it always also was (FREN151 ends at sequence 11) — it does no gating work anymore, since there's no gating left to do.
- **The method (and every call site) was left in place on purpose, not deleted.** Well over a dozen places across `game.py` and `minigames.py` ask "is this row's content available" as their one real question — the farm's own `render_farm()`/`_plot_classes()`, the Review tab's candidate filter, the proficiency test's and bonus section's availability checks, the cultural-notes panel, the liaison drill's sequence filter, and all four arcade minigames' `*_available()`/`_range_fully_unlocked()` checks. Every one of them keeps working completely unchanged, because the answer to "is it unlocked" is now just always "yes." This is exactly the shape the interrupted session's own docstring on `is_row_unlocked()` describes, and it's the right call: rewriting a dozen-plus call sites to remove a check that still returns the right answer would be needless churn for zero behavioural gain, and it would throw away the one seam a future date-driven or placement-test-driven gate (L4b, still unbuilt, still a separate item) would want to hook back into.
- **The locked-row UI/CSS is dormant, not removed, and this pass confirmed it actually stays dormant.** `.row--locked`, `.plot--locked`, the "opens when row N has all sprouted" lock note, and the disabled proficiency/bonus buttons are all still defined in `style.css`/`game.py` exactly as Milestone 6 left them — per the interrupted session's own reasoning, ripping out CSS/markup that a future gate might want back is a worse trade than leaving a few classes that simply never get applied anymore. This pass didn't just take that on faith: `tests/test_row_unlock.py`'s new `test_no_row_or_plot_ever_renders_as_locked` and `test_the_lock_note_is_permanently_hidden_now_that_nothing_locks` walk every one of the farm's 23 rows and 790 plots and confirm the dormant classes/attributes genuinely never fire, which is a real regression guard, not a restatement of the code.
- **What was actually broken, and what this pass fixed.** The interrupted session left the game.py change in place but never touched the test suite that had been written against the *old* locked-row behaviour, and the "L4a build note" its own docstring pointed readers to didn't exist yet — so `python3 -m pytest games/champ-de-mots/tests` showed 29 failures out of 553 tests, every one of them a test asserting some row was locked, some button was disabled, some candidate pool excluded a "still-locked" row, or some counter matched the old smaller catch-up-zone numbers (504 plots, not 790). Went through each of the 29 one at a time against one question: does this test still describe something true and useful about the new design?
  - **Sixteen were deleted outright**, because their entire premise — a row transitioning from locked to unlocked, or a row staying locked while another opens — no longer exists and has no honest residual form: `test_fren152s_rows_start_locked`, `test_a_row_opens_once_the_previous_row_has_all_sprouted`, `test_a_partially_sprouted_row_opens_nothing`, `test_unlocking_advances_one_row_at_a_time`, `test_seeds_in_the_previous_row_hold_the_gate_shut`, `test_next_due_plot_never_offers_a_locked_plot`, and `test_the_gate_is_ready_for_a_real_semester_without_a_date_concept` (all `test_row_unlock.py`); `test_bonus_section_never_affects_row_unlock_state`; `test_build_liaison_drill_excludes_a_locked_sequence` (its own fixture assumption, "at least one drill question targets a locked week," is now unsatisfiable); `test_proficiency_test_never_affects_row_unlock_state`; `test_weakest_topics_never_includes_a_locked_row` (its `next(p for p in state.plots if not state.is_row_unlocked(p.sequence))` fixture can no longer find a single matching plot); and `test_review_never_pulls_plots_from_a_locked_row_even_after_unlock_changes`/`test_review_does_not_change_row_unlock_state`. The "never affects row unlock state" trio (bonus/proficiency/review) share a specific reason for deletion rather than a mechanical rewrite: `is_row_unlocked()` is now a pure function of which rows exist, never touched by `review()`/`invalidate_unlocks()` in any way that could change its answer, so the assertion became unfalsifiable — it could never catch a real regression, only restate a mathematical certainty. Also deleted, found as stale collateral in the same file while doing this surgery rather than one of the original 29 failures: `test_row_unlock.py`'s `test_a_wrong_answer_never_re_locks_a_row`, which was still technically green but exactly as vacuous as the trio above (nothing can "re-lock" a row that never locks).
  - **Nine were rewritten into genuine regression guards for the new behaviour**, not just weakened until they passed — following the instruction's own worked example almost exactly: `test_frenl51s_eleven_rows_are_open_from_the_start` broadened into `test_every_row_is_open_from_the_start` (all 23 rows, not just FREN151's 11); `test_locked_plots_are_not_counted_as_needing_water` became `test_every_plot_is_available_and_due_on_a_fresh_farm` (790 plots, not 504); `test_locked_rows_render_as_locked_and_disabled` inverted into `test_no_row_or_plot_ever_renders_as_locked`; `test_a_locked_row_says_what_opens_it` + `test_the_lock_note_disappears_once_the_row_opens` collapsed into one `test_the_lock_note_is_permanently_hidden_now_that_nothing_locks`; `test_clicking_a_locked_plot_does_nothing` inverted into `test_clicking_a_plot_in_a_formerly_locked_row_opens_practice` (row 12 specifically, as a named regression case, not a generic "clicking works" duplicate of `test_farm_ui.py`'s existing coverage); `test_row_summary_counts_open_rows` became `test_row_summary_shows_every_row_open_from_the_start` (permanently "23 of 23", not a count that grows); `test_unlock_state_is_recomputed_after_loading_a_save` kept its shape but now asserts the cache recomputes to "still unlocked" either way, confirming `load_state()`'s `invalidate_unlocks()` call is still exercised even though its answer can no longer vary; `test_only_unlocked_weeks_notes_are_shown` (cultural notes) became `test_every_weeks_note_is_shown_now_that_nothing_is_locked`, checking sequence 17's note renders alongside sequence 1's where it used to be filtered out; and `test_review_candidates_only_pulls_from_unlocked_rows` became `test_review_candidates_pull_from_every_row_now_that_none_are_locked`, asserting row 12 and row 23 both now appear where row 12 used to be asserted absent.
  - **Two were narrowed rather than deleted or broadened**, because the underlying function they tested has a second, still-real guard clause once the row-unlock half of it is set aside: `is_bonus_section_available()`/`is_proficiency_test_available()` both call `state.is_row_unlocked(sequence)`, and since `_compute_unlocked()` only ever contains sequences that correspond to a real row in `state.rows`, a sequence that isn't a real row (999, used here) still correctly reads as unavailable — not because it's "locked," but because there's no such row to be open. `test_bonus_section_is_not_available_for_a_locked_week`/`test_starting_a_bonus_section_for_a_locked_week_does_nothing` and `test_proficiency_test_is_not_available_for_a_locked_week`/`test_starting_a_test_for_a_locked_week_does_nothing` were rewritten around sequence 999 instead of the formerly-locked row 12, which is a genuine bounds check rather than a weakened lock check.
  - **One was rewritten into a positive functional check rather than just flipping its assertion.** `test_locked_row_proficiency_button_is_disabled_and_does_nothing` became `test_a_formerly_locked_rows_proficiency_button_is_enabled_and_works` — rather than only asserting `button.disabled is False` (which `test_each_unlocked_row_gets_a_proficiency_test_button` already covers for every row, generically), it also dispatches the click and confirms a real test session actually starts for sequence 12, since the point of this specific test was always to verify row 12 by name, not just the disabled flag.
  - **One test's stale comment was fixed in passing, no assertion changed.** `test_polish.py`'s `test_the_due_counter_gets_the_singular_right` was already green (it derives its "exactly one plot left due" setup from live `due_plots()` state rather than a hardcoded count, so it never depended on the old locked/unlocked split), but its comment claimed leaving one plot unwatered "keeps row 12's gate shut" — a gate that no longer exists. Reworded for accuracy since it sits directly beside the tests being reworked in this same pass; no test logic touched.
- **`test_the_due_counter_reads_as_a_sentence_not_a_scoreboard` and `test_a_locked_row_shows_no_due_count`** (`test_polish.py`) needed the same treatment as the `test_row_unlock.py` counter tests: the fresh-farm due count moved from 504 (FREN151's catch-up zone only) to 790 (the whole farm, now that nothing gates any of it), and row 12's due-count note — previously always hidden because the row was locked — now renders its real count exactly like row 1's always did. The second one was renamed `test_a_formerly_locked_row_now_shows_its_own_due_count` to name what it's actually guarding against regressing.
- **No other stale logic found.** `minigames.py`'s four `*_available()`/`_range_fully_unlocked()` checks (Blitz/Racer/Boutique Dash/Café Rush) all still call `_farm.is_row_unlocked()` the same way `game.py` itself does, and by the identical reasoning above they're now always-true dormant checks rather than broken ones — `_lock_reason()` (the shared "this range isn't fully unlocked yet" tooltip builder) is unreachable in practice now, in exactly the same "kept, not deleted, for the same seam a future gate might reuse" spirit as `game.py`'s own dormant lock CSS. None of this needed a code change, and per this task's own scope, minigame code was left untouched.
- **`planning/TODO.md`'s L4a checkbox is checked off**; L4b (a general placement test — a separate, unbuilt feature) is untouched, as it should be. A new `changelog.json` entry was added ("Row-unlock pacing removed: every week is now open from day one...") since this is a real, player-visible behaviour change, not an internal refactor.
- **553 tests before this pass (524 passing + 29 failing) → 538 tests after, all green.** The net reduction of 15 is entirely accounted for by the 16 outright deletions above minus the one test collapsed from two (`test_the_lock_note_disappears_once_the_row_opens` merging into `test_the_lock_note_is_permanently_hidden_now_that_nothing_locks` alongside `test_a_locked_row_says_what_opens_it`) — no test was silently dropped without one of the dispositions described above. `flake8 games/champ-de-mots --extend-ignore=E501` is clean. `test_bonus_sections.py`'s pre-existing `test_placing_tiles_out_of_order_is_marked_incorrect` (unrelated to this change, and unmodified by it) self-skips whenever its randomly-chosen week's tile order happens to reverse into a palindrome — it showed as 537 passed + 1 skipped on one run of this pass and 538 passed + 0 skipped on another, which is that test's own pre-existing nondeterminism, not a flake introduced here.

**Milestone 35 — R2-L1b: a practice-progress ledger so every practice mode visibly counts.** The user's rule (ideas doc L1/L10/L29): "practice through games should show progress on respective topics... it should impact the player score" and "everything gives progress to something measured at the top." Before this, only farm watering moved anything visible; minigames, liaison, proficiency tests and bonus sections were self-contained sessions whose scores vanished on close.

- **One shared ledger, not eight scoreboards.** `game.py` owns `practice_ledger`, keyed by mode: `blitz`, `racer`, `boutique`, `cafe`, `gender`, `liaison`, `proficiency`, `bonus`. Each mode stands for its "respective topic" (Verb Racer = verbs, Boutique Dash = clothing, gender drill = noun gender...). Every answered question calls `record_practice(mode, correct)`, which updates a lifetime correct/total tally (so accuracy is real, and the gender drill finally has its own running accuracy, L10) and possibly earns a point. `minigames.py` receives the function through `configure()`'s new optional fourth argument (no circular import; a no-op when absent).
- **The "player score" is the sum of practice points**, shown as a new "Practice score" tile in the always-visible header block and as a "Practice progress" section of the progress dashboard (per-mode "N of M correct (P%) · X/100 points"). The farm's own measured stat ("plots growing / automated") is unchanged; the game had no player score before, and deliberately still has no combo-style scoreboard.
- **Anti-farming caps:** 1 point per correct answer, at most `PRACTICE_DAILY_CAP` (10) per mode per in-game day and `PRACTICE_MODE_CAP` (100) per mode for good. Wrong answers count toward accuracy only. Minigame internal scores (Blitz points etc.) stay their own session-local thing; only correct/total feeds the ledger, so a fast replay can't inflate the header.
- **No SRS mutation, on purpose.** Milestone 5's stance that only real watering moves intervals/`next_due` stands; a test snapshots every plot's SRS fields across a liaison answer, a bonus session and a Blitz answer. The gender drill is a farm-watering variant (it already grows its plot through `state.review()`); it additionally records into the ledger, capped like the rest, and Review-tab gender answers count too.
- **Saved and validated.** `get_state()` adds `practice_ledger` (only modes with at least one answer, so an untouched farm's payload stays tiny); `load_state()` runs `_validated_practice_ledger()`, which defaults non-dict input, ignores unknown modes, rejects bools/non-ints, clamps every count, keeps `correct <= total` and `points <= correct`, and caps daily/lifetime points, so a tampered or old save can't grant score. Four existing save-shape tests gained the new key.
- **Not done:** per-topic (catalog topic-id) granularity, and the L1 fifth minigame. The ledger is per practice mode because minigame questions don't uniformly carry a topic id; a finer split would be a follow-up.
- **Tests: 538 -> 556 (new `tests/test_practice_progress.py`, 18 tests), all green; flake8 clean.**

**Milestone 36 — V-AB-1: a real first-run visual-style picker (correcting a deviation from the user's request).** The user asked for "when you first load in you choose a visual style which can be accessed through settings." Milestone 32 had reinterpreted this as a silent High-def default with no chooser, without the user agreeing to that. This milestone builds the requested behaviour.

- **What it does.** When `localStorage["champ-de-mots-visual-style"]` is unset and the viewport is desktop-width (`min-width: 641px`, matching the switcher's existing "desktop-only" scope), `visual-style.js` opens a modal `role="dialog"` picker (`#visual-style-picker` in `index.html`) with the four options plus "Skip — use High-def". Any choice, Skip, or Escape stores the style, so the dialog never reappears; Settings still changes it later. Narrow viewports and browsers with unreadable storage skip the dialog and keep High-def (a blocked-storage browser would otherwise be re-asked every load).
- **No flash, no premature save.** The page is styled High-def behind the dialog via `applyStyleUnsaved()`; nothing is persisted until the player answers.
- **Keyboard/accessible.** First option is focused on open, Tab/Shift+Tab wrap inside the dialog, Escape = skip, focus returns to the previously focused element on close, visible focus outline, `aria-labelledby`/`aria-describedby`. The first-run tutorial (`shared/tutorial.js`) now starts through `ChampDeMotsVisualStyle.whenPickerDone()`, so the two never stack.
- **Honest wording.** The picker note and the Settings note state that the styles are restyles (colours, fonts, borders, shadows) rather than separate artwork, that High-def is the original look, and that nothing about content/gameplay changes. Low-poly/Text-based/Cartoon remain CSS-only chrome treatments, as Milestone 32 already explained.
- **Verified live** in the Claude Browser pane at 1024px: first run shows the dialog with focus on High-def and nothing stored; Tab reaches Cartoon; clicking it applies + stores `cartoon` and the tutorial then appears; Escape stores `highdef`; a reload does not re-ask. (Synthetic Enter did not trigger a click in that harness; buttons are native `<button>`s.) Also verified Milestone 35's Practice score tile and dashboard rows live, hitting the documented stale service-worker/HTTP cache trap (`game.py` served old until caches were purged and refetched with `cache: "reload"`). Zero console errors.
- **Tests: 556 -> 561** (five structural tests in `test_visual_style_switcher.py`; the JS itself is checked live, since the harness has no DOM for it), all green, flake8 clean.

- Commit + tag per milestone: `git commit -m "Milestone N: <name>" -- games/champ-de-mots/` then `git tag champ-de-mots-milestone-0N`.
- Python via Pyodide, plain HTML/CSS, no build step — the hub's default stack.
- Tests use the hub's standard fake-DOM harness (`tests/fakes.py`, `tests/conftest.py`), same shape as the other games.
- Personal project: not part of any dev-log trail. Hub-linked from the main page (no BCM tag on its title card, since it isn't coursework).

---

## 14. Addendum: Grading Leniency, Review Modes & Bonus Sections (v2)

*(Source: `GRADING-AND-REVIEW-UPDATE.md`, dropped 2026-09-02. Folded in here rather than kept as a separate file, same as the original design doc. Doesn't touch the core farming loop — it changes what happens inside a single watering interaction, plus adds two new screens alongside the farm.)*

### 14.1 Relationship to what's already built

Milestones 1-7 already shipped a first pass at lenient typed-answer checking (`answer_alternatives()`, `CONTRACTION_PAIRS`/`_contraction_variants()`, `NUMBER_REGIONALISMS`, `_strip_plus_annotation()` — see §12's build notes for each). This addendum's §14.2 formalizes and extends that into an explicit STRICT/LENIENT two-tier system with a catalog-level `accepted` array per item, rather than replacing it — Milestone 8 should read the existing grading code first and evolve it, not build a parallel system.

### 14.2 Grading system

**Two strictness tiers**, decided automatically from an item's shape (word count, `topic_type`) rather than hand-flagged per item:

| Tier | Applies to | Behaviour |
|---|---|---|
| STRICT | Single-word vocab, numbers, isolated conjugated forms | Exact match after normalization only. No synonym list. |
| LENIENT | Phrases, full-sentence translations, fill-in-the-blank grammar answers | Matches against a curated list of 3-5 accepted phrasings per item. |

**Normalization** (both tiers, before any comparison): trim/collapse whitespace, case-insensitive, strip trailing punctuation, contraction equivalence (both directions, via the existing lookup table), curly/straight apostrophe equivalence. French-input accent-sensitivity is a **toggle, default ON** (spelling accents correctly is an assessed skill) — see §14.4's decision on scope.

**Accepted-answer variants (LENIENT tier):** each LENIENT item's catalog record gains an `accepted` array (canonical answer + contraction variants +, where relevant, a reordered equivalent), auto-generated at content-build time rather than hand-authored for all 966 items. The array grows over time from real usage via the report button (§14.2.4), not by trying to anticipate every phrasing up front.

```json
{
  "id": "fren151-w7-grammar001",
  "prompt": "There is a theatre.",
  "accepted": ["Il y a un théâtre.", "Il y a un théâtre", "il y a un theatre"]
}
```

**14.2.4 — The "Report" button:** every written-answer prompt shows an "I think this should count" button once marked wrong. Tapping it stores `{item_id, submitted_answer, marked_correct_answer(s), topic_type, timestamp}` to a new `answer_reports` table in the existing Neon/Postgres backend (`app/`) — not the static JSON catalog, which stays a build artifact. No auto-accept; every report needs a human (the user's) judgment call before it's added to an item's `accepted` array and redeployed.

### 14.3 Failure feedback blurb

When an answer is marked wrong (after normalization/leniency checks fail), show a short card: **what it is** (one-line restatement — the `rule` field already covers this for grammar items), **memory tip** (short mnemonic/pattern hint), **why it matters** (how it recurs later). Phase 1 (build now): template-filled from existing catalog fields plus a generic per-`topic_type` template for "why it matters." Phase 2 (explicitly a stretch, not part of this build): a one-off Claude API call per item, cached in the database — worth doing once the catalog structure has stabilised, not before.

### 14.4 Cross-section review modes, weekly tests, and decisions on the open questions

Two new modes accessible from a "Review" tab, opt-in, don't affect plot unlock pacing:

- **Random Word Review** — pulls a configurable number of `vocab`/`phrase` items from any unlocked week, filtered to a minimum growth stage. Correct answers nudge the SRS interval slightly forward on the source plot rather than being untracked.
- **Grammar Review** — same mechanic, filtered to `topic_type: grammar`, biased toward fill-in-the-blank/conjugation prompts.

The source doc left four questions open (its §7). Decided here so Milestone 11/12 can build against a settled spec rather than stalling on them:

- **Proficiency-test gating:** informational only, no unlock-gating — matches the doc's own stated lean, and avoids the front-loading/burnout risk already flagged for this game (§3's wilting design already commits to no punitive mechanics).
- **Accent-toggle scope:** one global toggle (default ON), not a separate default for Random Review — it's already user-togglable per session per §14.2, no need for a second mode-specific default to maintain.
- **Report review workflow:** build the `answer_reports` table + a couple of backend query endpoints (list/filter reports) — no dedicated in-game admin UI page. Matches the doc's own lighter-weight suggested option: reports get triaged periodically by pasting the queue into a Claude Code session, not through a page in the game itself.
- **Minimum stage threshold for Review modes:** configurable per session (a simple in-page control), not fixed — low cost to build, and matches the doc's own phrasing ("a minimum growth stage (e.g. 'Sprout or higher' or 'only Automated ones')").

### 14.5 Weekly proficiency tests

One test per `sequence` entry (per taught week), covering every topic in that week regardless of what's currently planted/watered — a single fixed-length session (~15-20 questions) sampling vocab/grammar/phrases from that week. Purely informational (score + per-topic breakdown), per the gating decision above.

### 14.6 Bonus sections: sentence building

Each week keeps its existing plot count unchanged; a bonus section is added per week using only that week's own vocab + grammar. Three tasks per bonus sentence: (1) drag word tiles into correct order, (2) translate each tile individually (STRICT), (3) translate the assembled sentence as a whole (LENIENT). Sentences must be **original**, combining that week's own catalog items — never lifted from the workbook, same copyright-safety rule as the runtime question generator (§5). Aim for 1-2 bonus sentences per week, authored fresh (not reused from any other source) and stored in a new `bonus_sentences` field per week entry in the catalog.

### 14.7 Milestone build order

Continues the existing numbering (see §11's table) rather than restarting — the source doc's own numbered list maps its already-built steps (content catalog, plant-state model, question generator, farm UI, save-code hook, row-unlock pacing, polish) onto this game's existing Milestones 1-3 and 7-9 respectively. New work is Milestones 8-13: grading engine v2 → report button + backend table → failure blurb → review tab → weekly proficiency tests → bonus sentence sections. Each gets its own commit + tag per §13's convention.
## Screen-reader accessibility audit (Z15, site-wide goal, planning/TODO.md)

Static-analysis audit (grepping/reading `index.html`/`game.py`, no live screen
reader in this environment — same audit-only method the colorblind-safety
passes already established for this hub) of the achievements and settings
panels: toggle-button accessible names, panel role/heading semantics, focus
order on open/close, keyboard reachability of interactive elements, and
checkbox/label association.

**No real gap found — no change made.** This game's settings panel already
has a real `<h2 class="settings-panel-heading">` title, and its visual-style
switcher (Milestone 32) is the best-instrumented control in the whole hub for
this exact audit: the four style buttons sit in a `<div role="group"
aria-label="Visual style">` and each carries a live `aria-pressed` attribute
that `visual-style.js` keeps in sync with the actually-selected style —
confirmed by reading the file, not just assuming from the markup. Checked
against the same candidate list as every other game in this pass:
- Both toggle buttons (`#achievements-toggle-button`, `#settings-toggle-button`)
  already carry descriptive visible text, a sufficient accessible name on
  its own — no `aria-label` needed.
- The achievements/dashboard panels (`.dashboard-panel`) have no heading of
  their own, but are adequately labeled by their toggle buttons' own visible
  text immediately above them.
- No focus-trap exists anywhere in the achievements/settings toggle flow
  (the first-run visual-style *picker* modal is a separate, already-correct
  case — `visual-style.js` already restores focus to the previously-focused
  element on close and traps Tab inside the modal while it's open, which
  this game had already built before this audit even started); clicking the
  settings/achievements toggle buttons never moves focus itself, so it
  naturally stays on that same button when either panel opens or closes.
- Every interactive element inside both panels is a real `<button>`
  (confirmed via a grep for `.onclick =` assignments and
  `createElement("div")` calls with a wired click handler in `game.py` —
  none found).
- Every checkbox/select in the wider practice UI is properly labeled: the
  accent-sensitivity checkbox is wrapped by `<label class="accent-toggle">`,
  and the Review panel's "Items per session"/"Minimum growth stage" controls
  are each wrapped by `<label class="review-setting">`.

No code changed; ran as the baseline check this pass calls for regardless
of verdict.

## L8: plots-automated bar (2026-09-21)

A thin bar under the "plots growing / automated" tile shows automated plots as a share of the whole farm (`#automated-bar` inside `#automated-meter`, updated in `render()` next to the existing `progress-display` text; the title reads "N of M plots automated"). It deliberately has no width transition: `tests/test_polish.py::test_the_farm_has_no_animation` enforces the calm-farm rule, and this pass's first draft failed it until the transition was removed. Suite 562 passing (added one test in `tests/test_farm_ui.py`); checked live, no unexpected console errors.

## L26: liaison sound legend (2026-09-21)

The liaison/elision drill shows a one-line legend (`#liaison-legend`) for the bracketed sounds a question mentions. `liaison_legend_text(entry)` scans the prompt, choices and explanation for `[x]` symbols, keeps those in `LIAISON_SOUND_LEGEND` (z, t, n, r, p, each described by an English comparison) and hides the line when there are none. It is data-driven, so future drill questions using another listed sound need no code; today only the `[z]` question triggers it, which is why the legend is a one-liner rather than a full IPA chart. Suite 565 passing (3 new tests in `tests/test_liaison_drill.py`); checked live, the legend renders for the [z] question.

## L6: confidence accuracy stat (2026-09-21)

A one-line tally (`#practice-confidence-stats`) shows how often the player's "Sure" and "Not sure" ratings were right: `confidence_tally` (`[correct, total]` per tag) is updated in `submit_answer()` only when a confidence tag was set, and `confidence_stats_text()` renders it. It is session-only by design, the same posture as `combo_count`/`current_confidence` (a fresh page starts at zero and nothing goes into `get_state()`); making it persistent would need a save-shape change, so it was left as a possible follow-up. Suite 566 passing (1 new test in `tests/test_farm_ui.py`); checked live under Pyodide.

## L10: gender drill accuracy line (2026-09-21)

When the open question is a gender-tag question, a line under it (`#gender-accuracy-display`) shows the drill's running accuracy, e.g. "Gender drill so far: 2/3 right (67%)". It reads `practice_ledger["gender"]` (the Milestone 35 ledger, so it's saved and consistent with the dashboard's per-mode numbers) via `gender_drill_accuracy_text()`; it stays hidden for other question types and before the first answer. Suite 568 passing (2 new tests in `tests/test_gender_tag.py`). Not separately checked in a browser (same display pattern as L6/L8).

## L18: weeds tooltip names the look-alike (2026-09-21)

`weeds_confusions_for(plot)` looks the plot's own French and English text up in `WEED_CONFUSIONS` (the table that puts plots in the weeds) and `_plot_title()` appends "Easy to mix up with: “x”" while the plot is in the weeds and the table has an entry. It names the table's known look-alikes rather than storing which specific wrong answer the player typed, which would need a save-shape change (`get_state()` per-plot record). Suite 571 passing (3 new tests in `tests/test_weeds.py`). Not separately browser-checked (tooltip text only).

## L16: topic-type cue on farm plots (2026-09-21)

`_plot_classes()` adds `plot--type-<topic_type>` to every plot cell, and `style.css` gives grammar a dashed 2px border, phrase a dotted 2px border and phonetic a double 3px border (vocab keeps the plain solid one). It is a border-style cue rather than a colour, so it is colourblind-safe and independent of the stage colours; the cells stay 25x25. The four visual-style themes may restyle borders, so the cue is guaranteed only for the default look. Suite 572 passing (1 new test in `tests/test_farm_ui.py`); computed styles checked live for all four types.

## L12: next-review countdown on automated plots (2026-09-21)

`_plot_title()` appends "next review in N day(s)" to an automated plot's tooltip (and its aria-label, which shares the text) whenever `plot.next_due` is still in the future; once it is due the existing "ready for water" note takes over. Tooltip-only, so nothing is added to the 25px cells. Suite 573 passing (1 new test in `tests/test_farm_ui.py`); not separately browser-checked.

## L20: watered vs never-watered row readout (2026-09-21)

Each row's progress count (`row-progress-<n>`, still "grown/total") now has a tooltip separating plots watered at least once (`last_reviewed` set, which includes wrong answers) from plots never watered. Re-scoped per the TODO note: with row gating gone (L4a) it applies to every row. It is a tooltip on the existing count rather than a second visible number, to keep the row header uncluttered. Suite 573 passing (1 new test in `tests/test_farm_ui.py`); not separately browser-checked.

## L27: farm-health distribution on the dashboard (2026-09-21)

The Progress dashboard gained a "Farm health" section (top of the panel): one row per growth stage with its icon, name, plot count and percentage, plus a thin neutral bar. `dashboard_stage_distribution()` just counts existing `plot.stage` values (no new state); every row is spelled out in text so nothing relies on the bar or on colour. Suite 575 passing (2 new tests in `tests/test_progress_dashboard.py`); checked live at desktop width (5 rows, no overflow).

## L23: scheduling transparency explainer (2026-09-21)

A disclosure under the Progress dashboard (`#srs-explainer`, visible only while the dashboard is open) explains the SM-2-flavoured scheduler in seven plain lines. `srs_explanation_lines()` builds them from the real constants (`DEFAULT_EASE`, `MIN_EASE`/`MAX_EASE`, the interval and bonus constants, the confidence multipliers, the stage thresholds), so the text can't drift from the code, and a test asserts the numbers appear. The `<details>` element is static in `index.html` (only its body is refilled) so its open state survives re-renders. Suite passing (2 new tests in `tests/test_progress_dashboard.py`); checked live.

## L17: mixed review marathon (2026-09-21)

A third Review-tab button (`#review-marathon-button`, "Mixed Review Marathon") starts a long session over the whole farm. `marathon_candidates()` takes every plot that `is_due()` (any topic type, any row, any stage), puts plots that have been watered before first ordered by `next_due` (most overdue at the top), fills any remaining slots with never-watered plots, and caps the session at `MARATHON_COUNT = 40`; a mature farm gets a real catch-up session, a brand-new one still gets something to do. It reuses the existing Review machinery (`start_review(MARATHON_MODE)`, the same question flow, scoring, reports and gentle correct-answer nudge), deliberately ignores the count and minimum-stage controls (the tooltip says so), applies the grammar fill-in bias to grammar plots inside the marathon, and shows its own empty-state message when nothing is due. No new saved state. Suite 586 passing (`tests/test_review_marathon.py`, 8 tests); checked live ("1 of 40", no unexpected console errors). Possible follow-ups: a user-set marathon size, and marathon-specific practice-ledger credit.

## L13: opt-in study buddy (2026-09-21)

A "Study buddy: off/on" button in the stats row (`#study-buddy-toggle-button`) shows a suggested daily review length (`#study-buddy-display`) when on. `study_buddy_overdue_count()` counts already-watered plots that are due (never-watered seeds are excluded, or a fresh farm would suggest hundreds of reviews); `study_buddy_message()` suggests min(due, `STUDY_BUDDY_MAX_SUGGESTION` = 25) reviews at `STUDY_BUDDY_SECONDS_PER_REVIEW` = 30s each, points to the Mixed Review Marathon when more are due, and says "all caught up" when none are. It only ever suggests: nothing is blocked or scored, and there is no streak framing. The on/off preference is saved as `study_buddy: true` only when enabled (the default save is unchanged) and validated on load.

**Test-suite housekeeping (same pass):** `tests/test_changelog.py` caps `changelog.json` at 25 entries ("curate, don't dump"). The L17 marathon commit had pushed it to 26 without a full-suite rerun after adding its entry, so the suite was red; this pass merged the day's small display-detail entries into one grouped entry (17 entries now) and the full suite is green again. Suite 592 passing (`tests/test_study_buddy.py`, 6 tests).

## 320px mobile-viewport audit (Z18, site-wide goal, planning/TODO.md)

Checked at a genuine 320px viewport (narrower than the original 375px
mobile pass) via the Claude Browser tool's `resize_window`: tutorial,
Achievements, Settings panels, a live plot question card, and the Verb
Racer minigame panel (started a race, checked the track/marker layout),
plus a full-DOM `scrollWidth`/`clientWidth` sweep in each state. Audited,
no change needed — `document.documentElement.scrollWidth` never exceeded
320. The top `#mobile-hud-bar` strip's own `overflow-x: auto` is the same
deliberate scroll-not-clip idiom Canopy's Z18 section documents, not a
bug. The other three arcade minigames (Greetings & Basics Blitz, Boutique
Dash, Café Rush) share the same `.minigame-panel` shell already checked
clean here and weren't each individually played through.

## Difficulty-aware achievements audit (Z27, site-wide goal)

Checked `game.py` for a player-facing difficulty/hard-mode toggle. Two
candidates surfaced and both turned out not to qualify: **STRICT/LENIENT
grading tiers** (`§14.2`) are an automatic per-question classification
(single word/compound vs. a phrase or sentence) decided by `answer_
tier()`, not something a player chooses — every question always gets
graded at its own fixed tier regardless of any setting. **`ACCENT_
SENSITIVE`** is a real player toggle (via `on_toggle_accent_sensitivity()`)
that does make grading measurably stricter when on (accents must match
exactly rather than being folded/ignored), but it's session-only (never
persisted, resets fresh each load, matching the same category this file's
`ACCENT_SENSITIVE` comment already documents alongside L13's study-buddy
preference), freely toggleable at any time, and never makes a correct
answer unreachable — French accents are always objectively typable, so
turning accent-sensitivity on only demands more precision, never blocks
progress outright. Checked it against the achievement catalog
(`automated_25` through `row_23`, all built around cumulative plot-
automation counts via the SRS engine, none keyed to a raw accuracy
percentage or an answer-tier condition) and found no dependency on
grading strictness at all — automating a plot just takes correct answers
over time, accented or not.

**Le Champ de Mots has no difficulty/hard-mode toggle that gates
achievement reachability — not applicable.** No code touched.

## Z-extra/A26: reset-to-default settings button (2026-09-21)

A "Reset to Default" button (`#settings-reset-button`) sits at the bottom
of the Settings panel, below the visual-style control row — SOL's own A26
answer flagged a settings reset as a site-wide pattern rather than a
SOL-only feature, folded into `planning/TODO.md`'s Z-extra checklist. This
game's own Settings panel only ever carried the Milestone 32 visual-style
switcher (no text-scale/reduce-motion controls exist here — those were
never built for this game), so "reset to default" means one thing:
`applyStyle(DEFAULT_STYLE)`, returning the page to High-def. Implemented
entirely in `visual-style.js` (no Python touched, same "independent of
Pyodide" rule the rest of this file's visual-style build notes already
establish) — one click updates `data-visual-style` on `<html>` and every
style button's `aria-pressed` state immediately, and writes `"highdef"`
back to `localStorage` so the reset survives a reload rather than only
looking reset until the next render. No confirmation dialog — this is a
low-stakes, instantly-reversible display preference, not a destructive
action, so `shared/confirm-dialog.js` is deliberately not wired up here.

## Achievement-progress bar in toolbar (Z-extra/A10, site-wide goal)

Already satisfied — see the existing toggle-button label. This game's
`render_achievements()` sets `#achievements-toggle-button`'s `innerText`
to `"🏆 Achievements (N/M)"` (or `"Hide achievements (N/M)"` while open),
and that function already runs on every `render()` pass (not only from
the toggle handler), so the live count is visible in the toolbar before
the panel is ever opened. No code change needed here.

## "What's new since you played" banner (Z24, site-wide goal)

A new shared `shared/whats-new-banner.js` -- deliberately distinct from
this game's own in-game "What's New" changelog PANEL above (opt-in,
player-triggered, always shows the full history from scratch). The
banner instead appears automatically on page load, but ONLY for a
RETURNING player who has missed real entries since their last visit: it
diffs this game's `changelog.json` against
`localStorage["whats-new-seen:champ-de-mots"]` (the date string of the
newest entry already shown) and lists just the new ones, not the whole
log. A first-ever visit silently marks the current changelog as seen
rather than dumping the full history on a brand-new player. Reuses the
same `window.CHANGELOG_JSON` global this game's own changelog panel
already fetches -- no second network request. One `<script
src="../../shared/whats-new-banner.js" data-game-id="champ-de-mots">`
include, added right after `shared/last-played.js`'s own include. See
root `CLAUDE.md`'s Working notes for the full write-up -- shared
infrastructure, documented once there rather than duplicated across all
12 games' own files.

## Narrative log migrated to shared component (Z11, site-wide goal), and a new "My Reports" panel

`planning/TODO.md`'s Z11 line asked to build this game's "report-
confirmation flow (L14)" on a new shared narrative-log component. Checking
what L14 actually specifies first (per the task's own instruction, given
this hub's history of cross-round label collisions) found it already
fully satisfied by something unrelated to a narrative log at all: L14
("let the report-button flow show a short 'thanks, noted' confirmation
distinct from the normal question-feedback flow") is the pre-existing
`REPORT_SENT_LABEL`/`PRONUNCIATION_REPORT_SENT_LABEL` button relabel
(every report button already reads "Reported — thanks" and disables
itself, separate from the answer-feedback line) -- checked off in
`planning/TODO.md` back on 2026-09-21, before this session started.

Rather than force that unrelated, already-done feature to stand in for
this task, this session built the genuinely narrative-log-shaped feature
Z11's own fallback language asks for: **"My Reports"** (`📨 My Reports`
toolbar button → `#report-log-panel`), a small dated feed confirming every
correctness/pronunciation report actually sent this session, across all
10 of this game's `submit_*_report()` call sites (main panel, Review,
Proficiency, and Bonus's tile/sentence tasks, each with both a
correctness and a pronunciation variant). Built on the new shared
`shared/narrative_log.py` module (generalized from Continuum's own
"Chronicle" log; Thaw's scientist's log is the second integration, this
is the third): `_record_report_log_entry(payload)` is called right after
every `_dispatch_report(payload)` call (added once, via a single
`replace_all` across all 10 identical call sites), and appends
`{"day": state.current_day, "topic": payload["topic_type"], "text": ...}`
via `narrative_log.add_entry(report_log, entry, cap=REPORT_LOG_MAX)`
(capped at 20). "Dated" means `state.current_day` (this game's own
no-wall-clock day counter, Milestone 2) rather than a real calendar date
-- `game.py` is forbidden from touching any clock API at all (Milestone
7's own tested constraint), the same reason §14.2.4's report payload
itself never builds a timestamp (the backend's `created_at` covers that).
`REPORT_LOG_TOPIC_LABEL` maps the three fixed non-plot topic-type markers
(`pronunciation`/`bonus_tile`/`bonus_sentence`) to a readable phrase;
anything else (a plot's own `vocab`/`grammar`/`phrase`/`phonetic`
topic_type) falls back to a generic "a {topic_type} answer" phrase.

Rendering reuses this game's own established one-`<p>`-per-row panel
idiom (`render_changelog()`/`render_dashboard()`'s own convention) via a
`_build_report_log_row()` callback handed to `narrative_log.render()`,
rather than Continuum's multi-element `row`/`row-top`/`row-name`/
`row-blurb` structure -- internals are free once you're inside a game.
`.report-log-row` reuses `.changelog-row`'s exact styling (including the
same three per-theme legibility overrides in `visual-styles.css` that
Milestone 33 already added for `.changelog-row` against the Low-poly/
Text-based/Cartoon palettes, since a new row class inheriting the same
dark-on-dark text would hit the identical bug). `report_log` rides
`get_state()`/`load_state()` (only written once non-empty, matching
`practice_ledger`'s own "don't bloat an untouched save" rule), validated
entry-by-entry on load via `narrative_log.sanitize()` with a small
`_is_valid_report_log_entry()` predicate -- a malformed or missing key
drops just that one entry rather than failing the whole load. An old save
predating this feature has no `report_log` key at all, which `sanitize()`
already treats as an empty list.

`index.html`'s boot script fetches `shared/narrative_log.py` and writes it
into the Pyodide filesystem alongside `minigames.py`, and
`tests/conftest.py` gained the same `SHARED_DIR` `sys.path` insertion
Continuum's/Thaw's own conftest.py already have (this game's own
`import minigames` resolves through a different, self-contained
try/except-with-path-insertion fallback that doesn't cover a second,
unrelated shared-directory import). New tests:
`tests/test_report_log.py` (17 tests -- recording from both report kinds,
one-shot behavior, the cap, panel open/close and empty-state rendering,
and the full save/load round-trip including a malformed-entry drop case).
592 → 607 tests (one pre-existing, unrelated test self-skips on its own
documented nondeterminism); `flake8` clean. A `changelog.json` entry was
added since this is new player-visible behavior (unlike Continuum's/
Thaw's pure refactors, which needed none).

Verified live via the shared `hub-dev-server`: submitting both a
correctness report and a pronunciation report produced two correctly-
labeled entries; a real DOM `.click()` on the toolbar button (not just a
direct Python call) correctly opened the panel and showed "Day 1 —
Reported a vocab answer for “hello”."; the panel's text color was checked
distinct and legible across all four visual styles (High-def/Low-poly/
Text-based/Cartoon). Zero console errors throughout.

## Keyboard-shortcut convention: ?/Esc (Z4, site-wide goal)

Wired via the new shared `shared/keyboard-shortcuts.js` -- one script
include plus one `KeyboardShortcuts.init({panels: [...]})` call at the
end of `index.html`, listing this game's real toggle-button + hidden-
panel pairs: How to Play, Settings, Progress Dashboard, Achievements,
What's New, My Reports, and all four arcade minigames (Greetings &
Basics Blitz, Verb Racer, Boutique Dash, Café Rush), plus Liaison
Practice, Review, and Cultural Notes. `?` opens a small floating
shortcuts-help overlay; `Esc` closes it and clicks the toggle button of
whichever listed panel is currently open, reusing each panel's own
open/close logic (kept working correctly under all four visual styles,
which only restyle chrome, not the toggle-button/panel wiring itself).
The Practice/Proficiency/Bonus panels, the cafe-twist sub-panel, and the
Study Buddy display aren't in the list -- none is a simple show/hide
toggle with one dedicated button the way the panels above are; they're
row-unlock-gated content screens or inline hints shown/hidden by game
state.
